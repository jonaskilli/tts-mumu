# 今日修改记录# 今日修改记录（2026-07-28 ~ 202# 今日修改记录（2026-07-28 ~ 2026-07-29）

分支：`mumu_tts_github`
提交数：16（含合并 PR #1~#4）
版本号：`1.26.062507` → `1.26.072910`
打包结果：✅ GitHub Actions# 今日修改记录（2026-07-28 ~ 2026-07-29）

分支：`mumu_tts_github`
提交数：16（含合并 PR #1~#4）
版本号：`1.26.062507` → `1.26.072910`
打包结果：✅ GitHub Actions 自动打包成功并发布 Release

## 一、应用功能优化

### 1. 优化# 今日修改记录（2026-07-28 ~ 2026-07-29）

分支：`mumu_tts_github`
提交数：16（含合并 PR #1~#4）
版本号：`1.26.062507` → `1.26.072910`
打包结果：✅ GitHub Actions 自动打包成功并发布 Release

## 一、应用功能优化

### 1. 优化 TTS 界面和角色管理（`4de1c28`、`e2882fd`）
- 工具箱# 今日修改记录（2026-07-28 ~ 2026-07-29）

分支：`mumu_tts_github`
提交数：16（含合并 PR #1~#4）
版本号：`1.26.062507` → `1.26.072910`
打包结果：✅ GitHub Actions 自动打包成功并发布 Release

## 一、应用功能优化

### 1. 优化 TTS 界面和角色管理（`4de1c28`、`e2882fd`）
- 工具箱页面交互优化，`ToolBoxScreen` 调整布局（+30 行）。
- 底栏可见性控制逻辑重构，`ControlBottomBarVisibility` 精简（-# 今日修改记录（2026-07-28 ~ 2026-07-29）

分支：`mumu_tts_github`
提交数：16（含合并 PR #1~#4）
版本号：`1.26.062507` → `1.26.072910`
打包结果：✅ GitHub Actions 自动打包成功并发布 Release

## 一、应用功能优化

### 1. 优化 TTS 界面和角色管理（`4de1c28`、`e2882fd`）
- 工具箱页面交互优化，`ToolBoxScreen` 调整布局（+30 行）。
- 底栏可见性控制逻辑重构，`ControlBottomBarVisibility` 精简（-49/+59 行）。
- 底部# 今日修改记录（2026-07-28 ~ 2026-07-29）

分支：`mumu_tts_github`
提交数：16（含合并 PR #1~#4）
版本号：`1.26.062507` → `1.26.072910`
打包结果：✅ GitHub Actions 自动打包成功并发布 Release

## 一、应用功能优化

### 1. 优化 TTS 界面和角色管理（`4de1c28`、`e2882fd`）
- 工具箱页面交互优化，`ToolBoxScreen` 调整布局（+30 行）。
- 底栏可见性控制逻辑重构，`ControlBottomBarVisibility` 精简（-49/+59 行）。
- 底部导航 `PagerDestination`、`MainPager` 页索引调整。
- 中英文 `strings.xml` 文案同步更新。
- 涉及：`MainPager`、`PagerDestination`、`ToolBoxScreen`、`ControlBottom# 今日修改记录（2026-07-28 ~ 2026-07-29）

分支：`mumu_tts_github`
提交数：16（含合并 PR #1~#4）
版本号：`1.26.062507` → `1.26.072910`
打包结果：✅ GitHub Actions 自动打包成功并发布 Release

## 一、应用功能优化

### 1. 优化 TTS 界面和角色管理（`4de1c28`、`e2882fd`）
- 工具箱页面交互优化，`ToolBoxScreen` 调整布局（+30 行）。
- 底栏可见性控制逻辑重构，`ControlBottomBarVisibility` 精简（-49/+59 行）。
- 底部导航 `PagerDestination`、`MainPager` 页索引调整。
- 中英文 `strings.xml` 文案同步更新。
- 涉及：`MainPager`、`PagerDestination`、`ToolBoxScreen`、`ControlBottomBarVisibility`、`strings.xml`。

### 今日修改记录（2026-07-28 ~ 2026-07-29）

分支：`mumu_tts_github`
提交数：16（含合并 PR #1~#4）
版本号：`1.26.062507` → `1.26.072910`
打包结果：✅ GitHub Actions 自动打包成功并发布 Release

## 一、应用功能优化

### 1. 优化 TTS 界面和角色管理（`4de1c28`、`e2882fd`）
- 工具箱页面交互优化，`ToolBoxScreen` 调整布局（+30 行）。
- 底栏可见性控制逻辑重构，`ControlBottomBarVisibility` 精简（-49/+59 行）。
- 底部导航 `PagerDestination`、`MainPager` 页索引调整。
- 中英文 `strings.xml` 文案同步更新。
- 涉及：`MainPager`、`PagerDestination`、`ToolBoxScreen`、`ControlBottomBarVisibility`、`strings.xml`。

## 二、CI/CD 自动打包链路打通# 今日修改记录（2026-07-28 ~ 2026-07-29）

分支：`mumu_tts_github`
提交数：16（含合并 PR #1~#4）
版本号：`1.26.062507` → `1.26.072910`
打包结果：✅ GitHub Actions 自动打包成功并发布 Release

## 一、应用功能优化

### 1. 优化 TTS 界面和角色管理（`4de1c28`、`e2882fd`）
- 工具箱页面交互优化，`ToolBoxScreen` 调整布局（+30 行）。
- 底栏可见性控制逻辑重构，`ControlBottomBarVisibility` 精简（-49/+59 行）。
- 底部导航 `PagerDestination`、`MainPager` 页索引调整。
- 中英文 `strings.xml` 文案同步更新。
- 涉及：`MainPager`、`PagerDestination`、`ToolBoxScreen`、`ControlBottomBarVisibility`、`strings.xml`。

## 二、CI/CD 自动打包链路打通（核心工作）

### 2. CI 触发条件配置（`292536c`）
- `.github/workflows/release.yml` 的 `on.push.branches` 增加 `# 今日修改记录（2026-07-28 ~ 2026-07-29）

分支：`mumu_tts_github`
提交数：16（含合并 PR #1~#4）
版本号：`1.26.062507` → `1.26.072910`
打包结果：✅ GitHub Actions 自动打包成功并发布 Release

## 一、应用功能优化

### 1. 优化 TTS 界面和角色管理（`4de1c28`、`e2882fd`）
- 工具箱页面交互优化，`ToolBoxScreen` 调整布局（+30 行）。
- 底栏可见性控制逻辑重构，`ControlBottomBarVisibility` 精简（-49/+59 行）。
- 底部导航 `PagerDestination`、`MainPager` 页索引调整。
- 中英文 `strings.xml` 文案同步更新。
- 涉及：`MainPager`、`PagerDestination`、`ToolBoxScreen`、`ControlBottomBarVisibility`、`strings.xml`。

## 二、CI/CD 自动打包链路打通（核心工作）

### 2. CI 触发条件配置（`292536c`）
- `.github/workflows/release.yml` 的 `on.push.branches` 增加 `mumu_tts_github` 分支。
# 今日修改记录（2026-07-28 ~ 2026-07-29）

分支：`mumu_tts_github`
提交数：16（含合并 PR #1~#4）
版本号：`1.26.062507` → `1.26.072910`
打包结果：✅ GitHub Actions 自动打包成功并发布 Release

## 一、应用功能优化

### 1. 优化 TTS 界面和角色管理（`4de1c28`、`e2882fd`）
- 工具箱页面交互优化，`ToolBoxScreen` 调整布局（+30 行）。
- 底栏可见性控制逻辑重构，`ControlBottomBarVisibility` 精简（-49/+59 行）。
- 底部导航 `PagerDestination`、`MainPager` 页索引调整。
- 中英文 `strings.xml` 文案同步更新。
- 涉及：`MainPager`、`PagerDestination`、`ToolBoxScreen`、`ControlBottomBarVisibility`、`strings.xml`。

## 二、CI/CD 自动打包链路打通（核心工作）

### 2. CI 触发条件配置（`292536c`）
- `.github/workflows/release.yml` 的 `on.push.branches` 增加 `mumu_tts_github` 分支。
- push 到该分支且 `CHANGELOG.md` 有变更时自动触发打包。

### 3. 修复 gradle-wrapper.jar 缺失（`7ae6cb2`，PR #3）
- 仓库缺少 `gradle/wrapper/gradle-wrapper.jar`，CI 报 `Could not find or load main class org.gradle.wrapper.GradleWrapperMain`。
- 添加该二进制文件，并修改 `.gitignore`：`*.jar`# 今日修改记录（2026-07-28 ~ 2026-07-29）

分支：`mumu_tts_github`
提交数：16（含合并 PR #1~#4）
版本号：`1.26.062507` → `1.26.072910`
打包结果：✅ GitHub Actions 自动打包成功并发布 Release

## 一、应用功能优化

### 1. 优化 TTS 界面和角色管理（`4de1c28`、`e2882fd`）
- 工具箱页面交互优化，`ToolBoxScreen` 调整布局（+30 行）。
- 底栏可见性控制逻辑重构，`ControlBottomBarVisibility` 精简（-49/+59 行）。
- 底部导航 `PagerDestination`、`MainPager` 页索引调整。
- 中英文 `strings.xml` 文案同步更新。
- 涉及：`MainPager`、`PagerDestination`、`ToolBoxScreen`、`ControlBottomBarVisibility`、`strings.xml`。

## 二、CI/CD 自动打包链路打通（核心工作）

### 2. CI 触发条件配置（`292536c`）
- `.github/workflows/release.yml` 的 `on.push.branches` 增加 `mumu_tts_github` 分支。
- push 到该分支且 `CHANGELOG.md` 有变更时自动触发打包。

### 3. 修复 gradle-wrapper.jar 缺失（`7ae6cb2`，PR #3）
- 仓库缺少 `gradle/wrapper/gradle-wrapper.jar`，CI 报 `Could not find or load main class org.gradle.wrapper.GradleWrapperMain`。
- 添加该二进制文件，并修改 `.gitignore`：`*.jar` 规则下增加例外 `!gradle/wrapper/gradle-wrapper.jar`。

### 4. 修复 pluginManagement 缺 Google 仓库（`a92cc37`、`6324e67`，PR #4）
- CI 报 `Plugin [id: 'com.android.library', version: '8# 今日修改记录（2026-07-28 ~ 2026-07-29）

分支：`mumu_tts_github`
提交数：16（含合并 PR #1~#4）
版本号：`1.26.062507` → `1.26.072910`
打包结果：✅ GitHub Actions 自动打包成功并发布 Release

## 一、应用功能优化

### 1. 优化 TTS 界面和角色管理（`4de1c28`、`e2882fd`）
- 工具箱页面交互优化，`ToolBoxScreen` 调整布局（+30 行）。
- 底栏可见性控制逻辑重构，`ControlBottomBarVisibility` 精简（-49/+59 行）。
- 底部导航 `PagerDestination`、`MainPager` 页索引调整。
- 中英文 `strings.xml` 文案同步更新。
- 涉及：`MainPager`、`PagerDestination`、`ToolBoxScreen`、`ControlBottomBarVisibility`、`strings.xml`。

## 二、CI/CD 自动打包链路打通（核心工作）

### 2. CI 触发条件配置（`292536c`）
- `.github/workflows/release.yml` 的 `on.push.branches` 增加 `mumu_tts_github` 分支。
- push 到该分支且 `CHANGELOG.md` 有变更时自动触发打包。

### 3. 修复 gradle-wrapper.jar 缺失（`7ae6cb2`，PR #3）
- 仓库缺少 `gradle/wrapper/gradle-wrapper.jar`，CI 报 `Could not find or load main class org.gradle.wrapper.GradleWrapperMain`。
- 添加该二进制文件，并修改 `.gitignore`：`*.jar` 规则下增加例外 `!gradle/wrapper/gradle-wrapper.jar`。

### 4. 修复 pluginManagement 缺 Google 仓库（`a92cc37`、`6324e67`，PR #4）
- CI 报 `Plugin [id: 'com.android.library', version: '8.8.1'] was not found`。
- 在 `settings.gradle` 的 `pluginManagement# 今日修改记录（2026-07-28 ~ 2026-07-29）

分支：`mumu_tts_github`
提交数：16（含合并 PR #1~#4）
版本号：`1.26.062507` → `1.26.072910`
打包结果：✅ GitHub Actions 自动打包成功并发布 Release

## 一、应用功能优化

### 1. 优化 TTS 界面和角色管理（`4de1c28`、`e2882fd`）
- 工具箱页面交互优化，`ToolBoxScreen` 调整布局（+30 行）。
- 底栏可见性控制逻辑重构，`ControlBottomBarVisibility` 精简（-49/+59 行）。
- 底部导航 `PagerDestination`、`MainPager` 页索引调整。
- 中英文 `strings.xml` 文案同步更新。
- 涉及：`MainPager`、`PagerDestination`、`ToolBoxScreen`、`ControlBottomBarVisibility`、`strings.xml`。

## 二、CI/CD 自动打包链路打通（核心工作）

### 2. CI 触发条件配置（`292536c`）
- `.github/workflows/release.yml` 的 `on.push.branches` 增加 `mumu_tts_github` 分支。
- push 到该分支且 `CHANGELOG.md` 有变更时自动触发打包。

### 3. 修复 gradle-wrapper.jar 缺失（`7ae6cb2`，PR #3）
- 仓库缺少 `gradle/wrapper/gradle-wrapper.jar`，CI 报 `Could not find or load main class org.gradle.wrapper.GradleWrapperMain`。
- 添加该二进制文件，并修改 `.gitignore`：`*.jar` 规则下增加例外 `!gradle/wrapper/gradle-wrapper.jar`。

### 4. 修复 pluginManagement 缺 Google 仓库（`a92cc37`、`6324e67`，PR #4）
- CI 报 `Plugin [id: 'com.android.library', version: '8.8.1'] was not found`。
- 在 `settings.gradle` 的 `pluginManagement.repositories` 末尾追加 `google()` 和