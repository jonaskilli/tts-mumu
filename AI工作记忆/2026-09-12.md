# 2026-09-12（凌晨，承接 09-11 深夜装机迭代）

## 00:15 配置项音频参数弹窗「取消」移到按钮行最左（08b8a18，未推送）
- 用户装机截图指认：配置项音频参数弹窗的「取消」孤立在右下角。定位根因——`AudioParamsDialog` 的 buttons 槽只有「取消」（MD3 confirmButton 槽只能靠右），而「重置/应用」在共用的 `AudioParamsDimensionSection` 维度按钮行里（`Arrangement.End` 右对齐），两处都靠右 → 出现「重置 应用」右对齐 + 「取消」掉到下一行仍右对齐。
- 全 app 既有约定（`GlobalAudioParamsDialog.kt:51` 注释写死）：**取消（左）｜ 重置 · 应用（右）同一行**。配置项弹窗缺这条是因为 09-10 ②A 把重置/应用挪进了维度区。
- 改法（2 文件 +19/−5）：`AudioParamsDimensionSection` 新增可选参数 `leadingAction: (@Composable () -> Unit)? = null`，按钮行里 `if (leadingAction != null) { leadingAction(); Spacer(Modifier.weight(1f)) }` → 取消贴最左、weight Spacer 把重置/应用顶到最右；`AudioParamsDialog` 传入「取消」并把 `buttons` 槽留空（MD3 confirmButton 必填槽传空）。
- **调用方只有两个**（已 grep 确认）：`LogQuickPanel:704`（不传 leadingAction → null，按钮行维持纯右对齐，不受影响）、`AudioParamsDialog:252`（传取消）。
- 待用户说「推」→ push → CI → tag 打包。本地在 `d56d2e4` 之上新增 1 笔：**08b8a18**。

## 00:25 配置项 ⋮ 菜单「移动到子分组」改名「移动到其他分组」（261ae18，未推送）
- 用户问要不要改成「移动本项到其他子分组」（想要精简+语义最准）。评估后否掉该提案：它只点明对象、仍只写「子分组」，漏了选择器实际能干的 跨大分组 / 回大分组根目录，且比原名长 4 字。
- 关键事实：`Item.kt:327` 菜单点开的是**统一分组树选择器**（`GroupTreePickerDialog`，标题却叫「选择分组」），支持 同组换子分组 / 跨大分组 / 新建子分组 / 回根目录 四种去向 → 应用统称「分组」而非「子分组」。
- 用户 AskUserQuestion 拍板选 **「移动到其他分组」**（7字）：与选择器标题「选择分组」、分组菜单既有「移动到其他分组」口径一致；对象由 ⋮ 卡片入口天然隐含，不必加「本项」（且「本项」已被音频参数配置层占用，避免撞词）。
- 改名处仅此一处（全文 grep 确认；`MoveToSubGroupDialog.kt:40` 同名标题是已无调用方的占位文件，未动）。
- 本地待推增至 2 笔：**08b8a18 + 261ae18**。

## 00:45 选择分组弹窗改「高亮版」（f35bbe4，未推送）
- 用户看完选择分组（GroupTreePickerDialog）截图提三点不满：折叠后看不出选中、满屏灰圈像全没选、圈太大。先问方框/圆圈怎么改，用户两轮选择后说"**你来帮我出主意，我刚选的不算数**"→ 我出三案，用户拍板**高亮版**。
- **病根定性的话（记住）**：三个不满同源——给「容器」发了「选项」的控件（一级分组本质是路径段不是目的地）。
- 落地（整文件重写，+128/−169）：①一级分组行=纯容器（箭头+组名+`(配置项数)`，点行=展开/收起手风琴），不放任何选择控件；②目的地行（子分组/根目录/新建子分组/新建分组）无圈，**选中=整行 primaryContainer 底+文字 onPrimaryContainer**（MD3 选中容器色标准用法），新私有组件 `DestinationRow(selected,text,indent,onClick,leadingIcon)`；③折叠时内含选中的分组名染主题色（`holdsSelection`）；④"（根目录，不设子分组）"行仅无子分组分组显示（业务规则不变：含子分组禁回根目录防混放）。
- **注意行为变化**：无子分组的大分组不再能点行直接选中根目录，需展开后点"（根目录，不设子分组）"行（多一击换语义统一，已随方案说明）。
- 单选圈（RadioButton）在该文件已全清，import 同步清；经验：Grep/Glob 在大目录会 30s 超时，核对单文件用 Read。
- 本地待推增至 3 笔：**08b8a18 + 261ae18 + f35bbe4**。等用户说「推」。

## 00:55 编辑页音频参数区默认收起（7f4f6be，未推送）
- 用户：选择分组高亮版"真得到时候我看了再说"（打包验证，勿再追问）；新需求=**编辑页音频参数滑块区默认隐藏**（太占地、看不到下方插件信息）→ 反转 09-11「方案A：默认展开语速、永不全收起」定案。
- 用户第一轮没看懂术语方案（"看不懂"）→ 教训：**给目目讲交互方案必须配示意图+大白话，禁用"守卫/回调/软槽"等词**。补 SVG 示意后一次拍板 **A 案：再点同维收起**。
- 落地（2 文件 +12/−9）：①`AudioParamsDimRows` 展开维默认 `-1`=收起（三项都不显勾选高亮），`onSelect = { if (it == expanded) -1 else it }`，滑杆区包 `if (dim >= 0)`，收起态软槽补 bottom 4dp；②`SoftSegmentedTextToggle` **放开"重复点击不回调"守卫**（`if (index != selectedIndex)` 删）——其余三处调用点自查过全部安全：BackupDialog 自带 `if (profile != target)` 挡 resetTypes、LogQuickPanel/AudioParamsDimensionSection 幂等赋值。
- 范围铁律：**仅编辑页**；卡片⋮弹窗与日志快捷面板仍是"永远有一维选中"平铺（09-10「弹窗与日志面板排版必须同款」约定不破）。
- 本地待推增至 4 笔：**08b8a18 + 261ae18 + f35bbe4 + 7f4f6be**。

## 01:08 排序弹窗去空旷（d234777，未推送）
- 用户看主界面排序弹窗截图："有点太空旷了，你改一下"（直接指令，未要求提案）。
- 病因：`ListSortSettingsDialog` 把列表数量（调用方传的 `name`="582"）用 titleMedium 居中孤悬 + 筛选项 FlowRow 居中缩成一团，内容短、留白大。
- 改法：数量降为左对齐小字「共 N 项」（bodyMedium/onSurfaceVariant，标题下的辅助行）；FlowRow 左对齐 + `Arrangement.spacedBy(横8/纵4)` 替代逐项手调 padding；LoadingContent 纵向留白 8→4dp；清 Alignment import、加 Arrangement。
- **共用组件**：系统TTS 与 替换规则 两处排序弹窗都用它，一处改两处生效。降序复选框/取消/启动按钮行未动。
- 本地待推增至 5 笔：**08b8a18 + 261ae18 + f35bbe4 + 7f4f6be + d234777**。

## 01:22 建子分组标签去括号提示（d4e0a6c，未推送）
- 用户问「子分组名称 (支持 中文/男声 多级)」啥意思、说不要也行。含义=**名称含 / 时按 categoryPath 一次建多级**（如 `中文/男声` 建两层）；原写法难懂且把标签挤成两行。
- 处理：标签回归纯「子分组名称」，提示降级为输入框下方 supportingText 小字「名称含 / 时一次建多级，如 中文/男声」。三处统一：CreateSubGroupDialog / MoveToSubGroupDialog（无调用方占位）改写，**GroupTreePickerDialog 原本没提示、补齐同款**。
- 若用户仍嫌小字多余 → 删三处 supportingText 即可（一行/处）。
- 本地待推增至 6 笔：**08b8a18 + 261ae18 + f35bbe4 + 7f4f6be + d234777 + d4e0a6c**。

## 01:28 多级示例换用户实际用法（24b2c4e，未推送）
- 三处 supportingText 示例「中文/男声」→「**讯飞/女青年**」（用户拍板，贴合其实际分组：讯飞插件/女青年）。
- 本地待推增至 7 笔：**08b8a18 + 261ae18 + f35bbe4 + 7f4f6be + d234777 + d4e0a6c + 24b2c4e**。

## 01:31 排序弹窗改回简单调整（88c5958，未推送，推翻 d234777 大半）
- 用户对 d234777 不满："其实我愿意是简单调整一下，但是你大改了"。给前后对比 SVG 后拍板：**数量并入标题「排序（582）」跟在标题后 + 选项恢复原版居中**；只保留留白收紧（LoadingContent 8→4dp）。
- 撤销项：「共 N 项」小字辅助行、FlowRow 左对齐 + spacedBy、逐项 padding 删除——全回原样。
- **教训（记牢）**：目目说的"改一下"≠授权大改排法，凡涉及"怎么摆"的布局决策要先问最小改还是随便改；解释方案必须配图。
- 本地待推增至 8 笔：**08b8a18 + 261ae18 + f35bbe4 + 7f4f6be + d234777 + d4e0a6c + 24b2c4e + 88c5958**（d234777+88c5958 净效果≈只收紧留白+标题带数量）。

## 01:40 选择分组弹窗也改回小改（f2b6683，未推送，推翻 f35bbe4 高亮版）
- 用户："之前改的转为其他分组这个也是要在原版基础上小改，你重新出方案吧"——高亮版重写被判改过头。
- 用户关键追问（答对很关键）："一级分组如果没有子分组是不能手动选中啊，这怎么体现？"——高亮版把所有一级分组变纯容器，**无子分组的组失去直接点选能力**，这正是要撤的原因之一；原版规则：有子分组→圈灰禁用、无子分组→圈可点选中变绿。
- 定案（AskUserQuestion 拍板"按此动手"+ 圈保持 20dp）：**撤回高亮版重写回原版结构，唯一改动=某组折叠且内含当前选择（子分组/新建中）时组名染主题色**（`holdsSelection` + Text color 条件）；supportingText 多级提示保留。KDoc 记"高亮版当日撤销勿再引入"。
- 落地方式：整文件按原版逐行重写（Write 前须先 Read——本环境对未读文件直接 Write 会拒），+175/−123。
- **教训强化（第二例）**：用户连续两轮否掉"自作主张的大改"（排序弹窗、选择分组）——**"你改一下/重新出方案"一律先出最小改方案+示意图确认，结构级重写默认不做**。
- 本地待推增至 9 笔：08b8a18 + 261ae18 + f35bbe4 + 7f4f6be + d234777 + d4e0a6c + 24b2c4e + 88c5958 + f2b6683（f35bbe4+f2b6683 净效果≈原版+染组名）。

## 01:55 九笔全推送（远端 head=f2b6683）
- `git push` 成功（首次 push 实际已生效，重试显示 Everything up-to-date；coreutils 又抽风 tail 失败致 exit 127，链式命令被截断需重跑核实）。
- 编译验证 run **34630425725** 后台盯守中。用户只说「推送」未说打包 → 按分工等指示。
- CI **success**（build 2m26s，head=f2b6683，9 笔全绿）。等用户定打包。

## 02:05 跨机器迁移准备（ab48a74 + 记忆进仓库）
- **技术文档 18 篇回归仓库**（`技术文档/`，commit ab48a74，已推送；push 首次 HTTP 408 超时、重试成功）。纯文档未触发 CI（符合 test.yml 白名单预期）。**文档从此两处**：E:\TTS\技术文档（工作区）+ 仓库/技术文档（便携副本）——**以后更新两边同步，或以仓库为准**。
- **AI 工作记忆进仓库**：用户在"公开仓库会暴露个人习惯与裁定"知情后拍板照放（无密码无密钥）。位置=`AI工作记忆/`（MEMORY.md + 2026-09-07/09/10/11/12 日志 + README 恢复说明）。恢复方法：复制到新工作区 `.workbuddy/memory/` 即可。
- 仓库可见性核验：jonaskilli/tts-mumu **PUBLIC**（gh repo view）。技术文档 grep 过个人标记（目米/用户名/路径）无命中。
- 新机器恢复清单：①装 WorkBuddy+登录 ②clone 本仓库并设为工作区（或父目录）③把 AI工作记忆/*.md 拷到 `<工作区>/.workbuddy/memory/` ④git 登录（gh auth）⑤CI/打包流程照旧（tag 1.26.*）。
- **迁移包已推送完毕**：远端 head=**8dbeebc**（ab48a74 技术文档 18 篇 + 8dbeebc AI工作记忆 7 文件=记忆+README）。两处副本约定：仓库=便携基准副本，工作区=运行时副本，收尾两边同步。
- 用户问"改代码挪 WorkBuddy 云端"：查官方文档实锤 WorkBuddy=桌面工作台、操作本地文件，**无云端/手机跑编程任务的形态**（云端=资料库云文档）。已答：必须某台电脑（不一定是这台），可选换机 clone / 云电脑装 WorkBuddy / 本机清日志提速；手机看不了改码链路。

## 02:25 新机（WorkBuddy）接入完成
- 工作区 = `E:\TTS\云端tts`；仓库已 clone 于 `E:\TTS\tts-mumu`，分支 `mumu_tts_github`。
- 新机接入四步已走完：①读 AI工作记忆/MEMORY.md + 09-12 日志 ②`AI工作记忆/*.md` 已拷入工作区 `.workbuddy/memory/` ③`git ls-remote` 核对远端 head=**331805b**（本地 HEAD 同值、`git fetch` 已同步）④`gh run list` 最近 CI 全 success（最新 34630425725 编译验证绿）。
- 技术文档 18 篇确认在仓库 `技术文档/`（含 UI 唯一标准《MD3 UI 规范约定.md》）。
- 环境备注：本机 bash 初始 PATH 缺 `/usr/bin`，git/gh 命令前需 `export PATH="/usr/bin:/bin:$PATH"`。

## 02:32 从其他分组移入配置弹窗：全选键位置+图标（9f9f6ca，未推送）
- 用户截图指认：弹窗筛选键行（名称/标签/插件）里，全选键被 `Spacer(weight(1f))` 撑到最右，与「插件」之间空一大段。
- 两轮拍板：位置=**方案A 紧跟插件后面**（删 weight Spacer，靠行内 spacedBy(8dp) 自然排列）；图标=**候选1 标准全选图标**——DoneAll(✓✓)/Clear(×) 换成 **SelectAll/Deselect**（主列表多选同款口径；ListManagerScreen 里那对只是残留 import 无实际用例，但 import 能编译过证明图标存在）。
- 改动共 3 处 +4/−5：删 Spacer、换 imageVector、清 DoneAll import 加 Deselect/SelectAll。MD3 自查：无硬编码 sp/Color/弹窗样式。
- **新机环境坑复现**：fetch 后跟踪 ref 更新静默失败（本地 `origin/mumu_tts_github` 卡在 994080b，远端实为 331805b），`git log origin/..HEAD` 假报全历史待推；`.git/refs/remotes/origin/` 目录不存在导致 echo 直写也失败，**改 packed-refs 对应行修好（再次验证）**。教训：本机判断待推一律 `git ls-remote` 直问远端。
- 本地待推增至 3 笔：**2367fcf（新机接入日志）+ 9f9f6ca（全选键改动）+ e43d4ff（日志同步）**。等用户说「推」。

## 02:43 底部「删除/取消」排布：目目拍板保持现状（不动代码）
- 提问背景：`GroupEditContentDialog` 三键（移动到…/删除/取消）走 AlertDialog 官方按钮行靠右（confirmButton 先、dismissButton 后、FlowRow End），长文案「移动到 N 项到 XX」一行挤不下 → 删除/取消换行到第二行仍靠右。
- 出两案：A=删除·取消挪到按钮行最左、与「移动」同一行（=音频参数弹窗同款，需把三键塞进一个槽+weight Spacer）；B=保持官方靠右+文案缩成「移动到 N 项」。**目目选「保持现状不动」**。
- 结论：此弹窗底部维持 MD3 官方靠右、允许换行；**勿再提议挪左或缩文案**。

## 02:47 弹窗标题改「」内嵌（b468c19，未推送）
- 现状 `"${菜单名} - ${group.name}"` →「从其他分组移入配置 - 游戏」——**全 app 唯一用「 - 组名」破折号后缀的标题**；其余带名字的标题一律「」内嵌（先例 `ListManagerScreen:1954` `移动子分组「$subPath」到其他一级分组`；计数用 `移动启用配置 (N个)`）。
- 目目拍板**方案 A**：标题=**`从其他分组移入配置到「游戏」`**，实现 `"${stringResource(R.string.edit_group_content)}到「${group.name}」"`（不动 string 资源、不加占位符，与 app 既有内嵌写法一致）。方案 B（标题短句+正文首行「目标分组：X」）未采纳。
- 改动 1 行。**本地待推 6 笔**：2367fcf / 9f9f6ca / e43d4ff / cfd51df / 8440b49 / b468c19。

## 02:56 合并弹窗单选行 + 多选底栏去主题色（fc1f2a3，未推送）
- **图一「合并同类配置项到其他分组」**（ListManagerScreen:1160 起）三处目目拍板：
  1. 分组列表原是 `TextButton{Text(name)}` 堆叠 → **吃 TextButton 默认主题色变绿、无圆圈、居中孤零零**。改**圆圈单选行**（`RadioButton` + `bodyMedium` 常规字色 + `selectable` 整行可点），抄自 `GroupTreePickerDialog` 同款（间距用 4dp 而非其 2dp，守 MD3 第④条）。
  2. 行为改**方案B：先点圆圈选中 → 底部「合并」键确认**（原来点一下立即合并并关窗；合并会删源分组，加确认防手滑）；`confirmButton` 从空槽换成「合并」按钮，`enabled = mergeTargetId != null`。
  3. **套列表类宽版** `0.9f + DialogProperties(usePlatformDefaultWidth=false)`，标题「合并同类配置项到其他分组」不再折两行。
- **图二 分组多选底栏**（ListManagerScreen:2733 起，`selectionMode` 时的 bottomBar）：全选/导出原是 `TextButton` 默认主题色 → 绿；删除显式 error 红 → 绿红混排像两个主操作。**全选/导出 改 `onSurfaceVariant` 普通灰黑**（=插件管理/替换规则顶栏那套口径）；**顺带把同栏「转为子分组」也改灰黑**（同栏统一，避免绿灰混排；超出目目原话但属同一意图，已在回复中声明）。
- 实现：+72/−26；新增 import `foundation.selection.selectable` / `material3.ButtonDefaults` / `material3.RadioButton`。自查：花括号/圆括号平衡、合并逻辑体全文件仅 1 份、无硬编码 sp/Color。
- **本地待推 8 笔**（新增 fc1f2a3）。等目目说「推」→ 推 + 盯 CI（代码笔会触发编译验证）。

## 03:1x 选项弹窗全量统一为「圆圈单选+底部确认」（11ad9e9，未推送）
- 起因：目目问「图一改前改后哪个好？因为我刚发现有其他菜单也这样」→ 我给出判断（改后好在①看得出是选择题②防手滑③与「选择分组」同款；改前省一次点击），并列出**同类写法还有 6 处**。
- 目目两轮拍板：**图一保留改后（圆圈+确认）**、**其他 5 处一起统一**、「选择操作」不动。
- 落地清单（全在 `ListManagerScreen.kt`，每处 = 状态变量 `xxxTargetId: Long?` + `RadioButton` 行（`selectable` 整行可点、`bodyMedium` 常规字色、间距 top/bottom 4dp）+ `confirmButton` 从空槽换成带 `enabled = 选中!=null` 的动作键 + 原「点行即执行」的逻辑整体搬进确认键）：
  1. 「移动子分组」(showExtractSubGroup) → 键名「移动」
  2. 「转为子分组」(showConvertToSubGroup 单入口) → 键名「转为子分组」
  3. 「转为子分组」(showConvertToSubGroupMulti 多选入口) → 键名「转为子分组」；原先把「取消」放 confirmButton 槽（因当时无正向操作）的注释与写法已改为**确认键=转为子分组、取消移入 dismissButton**
  4. 「移动启用配置 (N个)」→ 键名「移动」；**选中项用 `Pair<Long,String>`（组id + 子分组路径，空串=组根）**，因为该弹窗目标既可是一级分组也可展开后的某子分组；maxOrder 从 `models.find{...}` 现算
  5. 「移动子分组 (N/M)」→ 键名「移动」；**上区源子分组多选（TriStateCheckbox+Checkbox）保持勾选不动**，只改下区目标分组
  6. 「移动子分组「XX」到其他一级分组」→ 键名「移动」
- 「选择操作」(重启/刷新克隆列表) 按拍板**不动**，仍是绿字按钮堆叠（全文件仅剩它 1 处 `confirmButton = {}`）。
- 自查：花括号 1065/1065、圆括号 1735/1735 平衡；`RadioButton(`/`.selectable(` 各 8 处；`forEach{...TextButton(` 模式已 0 命中；MD3 第④条（间距 4dp）已守。
- **遗留小不一致（待目目定）**：这些弹窗底部都有「新建一级分组」行按钮，仍是 TextButton 主题色绿（属"动作"非"选项"，本次按最小改动未动）；同栏还有 0.92 宽版是 09-11 终裁，未改。已向目目报备。

## 环境/流程备注（本机）
- 待推计数以 `git ls-remote origin mumu_tts_github` 为准；本地跟踪 ref 会失真（见 02:32 条），必要时改 `.git/packed-refs` 行。

## 03:35 十一笔全推送（远端 head=a322897，CI 绿）
- `git push` 一次成功（无 SIGTERM）：`331805b..a322897`，`git ls-remote` 复核远端 = **a322897**。
- 编译验证 run **34665219871** 已成功（build 2m13s，含 `Compile Check`）。**代码 5 笔全绿**（9f9f6ca 全选键 / b468c19 标题 / fc1f2a3 合并弹窗+底栏 / 11ad9e9 六处弹窗统一 + 其余日志笔）。
- 目目只说了「推送」，未提打包 → 按发布分工**停下等指示**。若要出包：推 tag `1.26.0912xxxx`（release.yml 监听 `1.26.*`）最合适——本次含 6 个弹窗的交互改动，**适合装机实测确认手感**（尤其"选行→确认"这步）。
- 遗留待拍板：各弹窗底部「新建一级分组」行按钮是否也从主题色绿改灰黑（我推荐保持绿=动作色）。

## 09:46 批量修改配置改名+文案精简 + 插件卡片菜单重排（d8e845f，未推送）
- 目目截图指认（图一=主界面右上角该菜单弹窗，图二=插件卡片 ⋮ 菜单）+ 三轮 AskUserQuestion 定案：
  1. **改名**：「批量修改来源字段」→**「批量修改配置」**，**菜单项（MenuMoreOptions.kt）+ 弹窗标题（BatchSourceFieldsDialog.kt）同步改**。根因诊断：标题只涵盖"来源插件"一类，实际能改 启用状态/采样率/来源插件 三类 → 这是"深奥"的来源。
  2. **删掉「启用状态」整行**（目目先问"有啥用"，我举证用途=批量全开/全关一批配置项后，他仍选删）：去掉 label + 三个文字键 + `enabledSel` 状态；**onApply 签名去掉 `enabled` 参数**，调用处（ListManagerScreen onApply）里 `if (enabled != null) { vm.updateEnabledBatch(...) }` 整块删除。**`ListManagerViewModel.updateEnabledBatch` 现无调用方=死代码，暂留未删**（已向目目报备，待定）。
  3. **文案精简（目目明示偏好：菜单解释太长，要精简准确）**：
     - 作用域行 `作用域：X，选中范围内共 N 项。未选择的维度保持原值。` → **只留 `作用域：X`**；`匹配 N 项` 挪到**插件框正下方**当小字（`targetCount = pluginItemCounts[选中插件]`，数字本就是在说该插件有多少项）。
     - `采样率（自动识别=由音频头探测）` → `采样率`。
     - `来源插件（把配置项改指向另一个插件，发音人等字段保持原值）` → `来源插件`。
  4. **插件卡片菜单重排**（PluginManagerScreen 第11项 DropdownMenu，目目拍板）：上栏 = **设置变量(有才显示) / 编辑元数据 / 插件音频参数 / 按插件音色分类入库 / 切换引用配置 / 导出**；下栏 = 清空缓存 / 删除。= 插件音频参数提到编辑类之后、**切换引用配置从下栏提到上栏**、**导出移到上栏最后一格**。目目原话三条："插件音频参数放第二个、导出放上栏最后一个、切换引用配置也得放上面放导出前"，追加"如果有设置变量那就排第一个"。
- 改动 4 文件 +49/−63；三个改动文件花括号/圆括号均平衡、菜单项无重复、旧名与 enabledSel 无残留（仅 KDoc 保留改名说明）。
- 本地待推 1 笔（d8e845f）。等目目说「推」。

## 10:05 三处改名 + 新增「批量删除配置项」（350f0bb，未推送）
- 目目拍板：
  1. **「编辑元数据」分两处改名**：插件处=**「修改插件信息」**、朗读规则处=**「修改朗读规则信息」**（名字分开避免串）。每处三同步：弹窗标题 + 卡片 ⋮ 菜单项 + leadingIcon contentDescription（`PluginManagerScreen.kt` / `SpeechRuleManagerScreen.kt`；相关注释一并更新，用脚本批量替换后 grep 复核）。
  2. **「切换引用配置」→「切换引用配置项至其他插件」**：菜单项 + 弹窗标题（原「切换引用配置到其他插件」）+ 加载文案（原「正在切换引用配置…」）三处。
  3. **新增「批量删除配置项」**：位置=主界面右上角 ⋮「批量」组，紧跟「批量修改配置」（MenuMoreOptions 新增参数 `onBatchDeleteConfigs`，图标 DeleteForever）。**新文件 `BatchDeleteConfigDialog.kt`**（AppDialog：插件下拉 + 「匹配 N 项」+「作用域：X」+ 红色「删除后不可恢复」；按钮=取消 / 删除(红)、无插件时禁用）。
- **关键决策（目目拍板）**：删除下拉**不含「全部（不按插件筛选）」**（防一手滑删空整个池），实现=调用处 `batchPluginOptions(...).filter { it.first.isNotEmpty() }`；作用域口径与「批量修改配置」一致（搜索态=搜索结果）；执行=`dbm.systemTtsV2.delete` + `SystemTtsService.notifyUpdateConfig()` + toast「已删除 N 项」。
- 关于被他删掉的「批量启用/停用」：我举证其用途（=把某插件来的配置项一次性全开/全关；作用域=当前池中来源为该插件的项），他最终**不加回**，改为要这个批量删除。**`ListManagerViewModel.updateEnabledBatch` 仍是无调用方死代码（未删，已报备）**。
- 零件复用：`batchPluginOptions` / `batchPluginItemCounts` / `filterByPluginId`（ListManagerScreen 内 private）。AppSpinner 对空列表安全（内部 `entries.getOrNull(max(0, values.indexOf(value)))` 兜底）。
- 改动 5 文件 +155/−20。本地待推 1 笔（350f0bb）。等目目说「推」。

## 10:23 插件菜单顺序微调 + 批量删除弹窗改名去提示（415ee08，未推送）
- 目目追加三条（第 1 条推翻他前一条"设置变量排第一"）：
  1. **「修改插件信息」排到「设置变量」之前** → 插件卡片 ⋮ **上栏最终顺序：修改插件信息 / 设置变量(有才显示) / 插件音频参数 / 按插件音色分类入库 / 切换引用配置项至其他插件 / 导出**；下栏 = 清空缓存 / 删除。
  2. **「批量删除配置项」→「批量删除插件配置项」**：菜单项 + 弹窗标题同步改，注释同改。
  3. **删掉该弹窗里「删除后不可恢复」提示**（原红色 labelMedium 那行）；随之清理失效 import（`foundation.layout.padding`、`unit.dp`）。
- 改动 4 文件 +17/−24；括号平衡与 import 自查通过。
- **本地待推 6 笔**（3 代码：d8e845f / 350f0bb / 415ee08；3 记录：dd56b07 / 0656cb1 / 28f560d）。等目目说「推」。

## 10:30 失效清理 + 删除清单 + 切换前校验（368b565，未推送）
- 目目三问（截图/口述）：
  1. **「清理全部失效配置项」**：加进**失效配置项弹窗底栏左下**，右下仍是「关闭」（先答错一次：他说"不是这样加…我现在想清理全部失效配置项"= 要**全部、不分来源**）。实现走 `vm.batchDeleteInvalidItems(null)`（**VM 本就支持 null=全部**，无需改 VM），删后 `context.toast("已清理 $invalidCount 项失效配置项")` + 关窗。
  2. **批量删除插件配置项弹窗：把待删项列出来**（目目："应该学学这个能列出来全部的配置项"）：新增数据类 `BatchDeleteEntry(pluginId, groupLabel, name, voice)`；弹窗内按**实际分组**（大分组，或「大分组 › 子分组」）`groupBy` 折叠展示，**默认全收起、点名展开**看「项名 · 音色」；换插件重置展开态。调用处 `deleteEntries` 由 `models.flatMap{...}` 构建（只收插件来源项，本地TTS 无 pluginId 不入）。**scopeDesc 恒为「当前池全部配置项」**（原按搜索态切文案，但清单取自全部 models → 提示与实际删范围会不一致，顺手改恒定）。
  3. **切换引用配置项：切换前按 voice 校验**（目目问"你是怎么校验的，按voice吗？"→ 答：是，`source.voice` 音色id 去目标插件音色集合里找）。新增两个文件级 helper：
     - `loadPluginVoiceIds(context, plugin)`：`TtsPluginUiEngineV2` → `eval()`/`onLoad()` → `getLocales().keys` 遍历所有池子 → `getVoices(poolId)` → 汇总 `voice.id`（**须在 IO 线程**，同「按插件音色分类入库」用法）。
     - `voiceIdOf(tts)`：取配置项的音色id。
     `pendingSwitch` 一设置即 `LaunchedEffect` 扫全库待切换项 + 拉目标音色清单（`withIO`）；确认弹窗三态：**校验中（转圈+"正在检查目标插件有没有这些音色…"）** → 「能匹配上 N 项 / 匹配不上 M 项（列出项名·voice）」→ 按钮「取消 / **仍然全部切换**（仅有不匹配时出现，按目目拍板保留兜底）/ 只切换能匹配的 N 项」。
- 已当面说明局限：按 voice id 比对最适合"**同一插件家族改名/重装**"（id 一致→全匹配）；**跨厂商音色id体系不同→大概率全不匹配**，故必须留硬切口子。
- 改动 3 文件 +298/−60；括号平衡与符号自查通过。

## 10:59 三处切换统一 voice 校验 + 撤硬切口子（3104f06，未推送）
- 目目追加三条：①**不做「按名字匹配」兜底**（"voiceid 对不上就别切了，切了也用不了"）②**撤掉「仍然全部切换」**按钮 ③**三处都统一**（我扫出"换来源插件"能力实际有三处：插件卡片菜单 / 顶栏失效配置项切换 / 批量修改配置的「来源插件」段，此前只有第一处有校验）。
- **新增共用件 `list/PluginVoiceMatch.kt`**：
  - `internal fun voiceIdOf(tts)`：取配置项音色id。
  - `internal fun loadPluginVoiceIds(context, plugin)`：跑 `TtsPluginUiEngineV2`（eval→onLoad→`getLocales().keys` 遍历池子→`getVoices(poolId)`）汇总全部音色id，**须 IO 线程**（同分类入库用法）。
  - `@Composable internal fun SourceSwitchCheckDialog(title, message, items, targetPluginId, targetPluginName, onDismiss, onConfirm)`：内部自己 `LaunchedEffect` 查目标插件（`dbm.pluginDao.getByPluginId`）+ 拉音色清单，三态=校验中(转圈)/读清单失败(红字"已停止切换")/结果(「能匹配上 N 项」绿 + 「匹配不上 M 项」红+逐条「项名 · voice」)；确认键=「只切换能匹配的 N 项」(enabled=有命中)，**无硬切**。
  - `onConfirm` 只回传命中的项，落库由各调用处自理。
- 三处接入：
  1. **PluginManagerScreen（插件卡片菜单）**：删掉自己那份弹窗与本地 helper，改调共用弹窗；scan 只保留"扫出引用源插件的项"（`switchCandidates`，`switchVoiceIds` 状态删除）。
  2. **ListManagerScreen（顶栏失效项切换）**：原 `AlertDialog(batch_fix_confirm)` 换成共用弹窗；`fixItems` 来自 **VM 新增的 `invalidItems: StateFlow<List<SystemTtsV2>>`**（与 invalidSourceItems 同源计算，全库不受搜索影响——修复清单若取自 models 会被搜索框缩小导致漏切）；执行走 `vm.batchFixInvalidItems(newPluginId, sourceId, onlyIds=命中id集)`（**VM 加 `onlyIds` 白名单参数**）+ toast。
  3. **批量修改配置「来源插件」段**：选了目标插件 → 不再直接落库，先 `pendingSourceSwitch = PendingSourceSwitch(items, newPluginId, sampleRate)` 过共用弹窗；确认后**采样率对整批生效、切换只作用于命中项**（两个维度独立，已在弹窗注释说明）。VM `updateSourcePluginBatch` 不用改（传命中的子集即可）。
- 复用细节：新数据类 `PendingSourceSwitch`（ListManagerScreen 文件底部 private）；PluginManagerScreen 清掉本地 helper 与失效 import（heightIn）。
- 自查：4 文件括号平衡；「仍然全部切换」/`switchVoiceIds`/`batch_fix_confirm`/`batch_select_plugin` 全 0 残留。**本地待推 10 笔**（5 代码 + 5 记录）。等目目说「推」。

## 11:05 第二批 11 笔推送（远端 head=bf62885，CI 绿）
- 目目「推」→ `a322897..bf62885` 一次成功，`git ls-remote` 复核 = **bf62885**。
- 编译验证 run **34669493824** 已成功（build 2m11s）。**代码 5 笔全绿**：d8e845f 改名+文案精简+菜单重排 / 350f0bb 编辑元数据等改名+新增批量删除 / 415ee08 顺序微调+弹窗改名 / 368b565 失效清理+删除清单+切换校验 / 3104f06 三处统一 voice 校验。
- **今天两批共 22 笔全部过 CI，无一次编译失败**。远端=本地，无待推。
- 待目目后续反馈（装机实测点）：
  - 六个选项弹窗的「圆圈单选+确认制」手感
  - 批量删除弹窗的分组折叠清单
  - 三处切换的 voice 校验：读音色清单的等待时长是否可接受（跑插件引擎，可能几秒）
- 若目目要出包：推 tag `1.26.0912xxxx`。

## 11:13 批量调整音频参数弹窗精简（f69a025，未推送）
- 目目指认（截图）：①「重置为 1.0」→**「重置」**；②插件栏下方两行长提示（作用域含「选中范围内共 N 项」+「单条语速/音量/音高将被统一替换」）→ **精简为「匹配 N 项」（插件框下）+「作用域：当前池全部配置项」**，与「批量修改配置」完全同款。
- 全 app grep 确认「选中范围内共 / 将被统一替换 / 重置为 1.0」已 0 残留。
- 改动 1 文件 +8/−3。本地待推 2 笔（f69a025 + 日志 e5c0c7e）。等目目说「推」。

## 11:22 替换规则默认分组按主界面口径（简化版）（477a2e6，未推送）
- 目目指认：替换规则的默认分组要按主界面默认分组处理来，但是**简化版**——替换规则没有子分组、也没有各种移动功能。
- 对比结论：主界面口径=「默认分组永远留库兜底 + **空则不显示**」（ListManagerViewModel:141）；替换规则已有"init 确保默认分组存在"的兜底，**缺的只有"空则不显示"** → 在 `ReplaceRuleManagerViewModel.updateSearchResult` 开头加 `visible = src.filter { it.group.id != DEFAULT_GROUP_ID || it.list.isNotEmpty() }`，搜索分支也改为遍历 visible。
- 改动 1 文件 +6/−3。**本地待推 4 笔**（2 代码：f69a025 / 477a2e6；2 记录）。等目目说「推」。

## 11:27 选中控件统一 + 菜单图标排查（6b72ce4，未推送）
- 目目两条：①各处"选分组"的前置选中控件方框/圆圈混乱，找统一方式；②各 ⋮ 菜单 leadingIcon 太类似，排查修改。
- **选中控件排查结论**：规则=圆圈(单选)/方框(开关+多选)/箭头(展开)；今天改过的选目标分组弹窗已一致；**唯一实质违规=list/Item.kt 的启用开关**——分组行(GroupItem)与一级项用 Checkbox，**子分组项却用 RadioButton 当开关**（role=Switch，纯历史遗留）→ 目目拍板**全用方框**，改 Checkbox（行为不变），RadioButton import 随之清除（isInSubGroup 参数暂留未删，无调用影响）。
- **图标排查结论（目目标准=「相邻不混淆」即可）**，改 4 组：
  1. 分组 ⋮「从其他分组移入配置」→ **MoveToInbox**；「合并同类配置项到其他分组」→ **CallMerge**（原与「移动子分组」三连同用 DriveFileMove，后者保留=真"移动"）。
  2. 分组 ⋮「删除启用的配置/删除未启用的配置」——**保留 DeleteForever 成对**（相邻同图标但为对称对，不属混淆；未按原表改 DeleteSweep，因目目只要求相邻不混淆）。
  3. 插件 ⋮「设置变量」→ **Tune**（原与相邻「修改插件信息」同用 EditNote）；PluginEditorScreen 的设置变量按钮同步 Tune。朗读规则/替换规则菜单无此重复。
  4. 插件 ⋮「按插件音色分类入库」→ **LibraryAdd**（原 Input 与同菜单「导出 Output」易混；顶栏"导入配置"的 Input 不动）。
- 主界面 ⋮ 的「批量删除插件配置项」DeleteForever 保留（相邻无同类）。
- 改动 4 文件 +42/−54；括号平衡自查通过。**本地待推 8 笔**（4 代码：f69a025 / 477a2e6 / 6b72ce4 + 3 记录中 4 笔）。等目目说「推」。

## 11:47 第三批 8 笔推送（远端 head=4a473a5，CI 绿）
- 目目「推」→ `bf62885..4a473a5` 一次成功，`git ls-remote` 复核 = **4a473a5**。
- 编译验证 run **34671397959** 已成功（build 2m11s）。代码 3 笔全绿：f69a025 批量音频参数弹窗精简 / 477a2e6 替换规则默认分组简化版 / 6b72ce4 选中控件统一+菜单图标。
- **今天三批共 30 笔全部过 CI，零失败**。远端=本地，无待推。
- 仍开放的事项：①出包（目目要的话推 tag `1.26.0912xxxx`）②装机实测清单见 11:05 条 + 本批的启用开关长相/新图标观感。

## 11:53 修复快捷栏「更换旁白发音人」显示无（73d5f9d，未推送）
- 目目截图：日志快捷栏「发音人调整」→ 更换发音人页签显示「旁白分类没有可用的配置项」，但旁白分组明明 113 项。
- **根因（实锤）**：标签存两字段——`speechRule.tag`=标签 **id**、`speechRule.tagName`=显示名（列表角标读 tagName）；`LogQuickPanel.kt` 旁白候选过滤却写 `speechRule.tag != "旁白"`，而 `reassignNarrationTags`（ListManagerScreen:744）批量分配旁白标签写的是 **tag="narration"、tagName="旁白"** → id 永远不等于中文显示名 → 整列筛空。
- **修复**：候选过滤改为 `tag=="旁白" || tagName=="旁白"` 任一命中（与 ListManagerScreen:742 的检测口径一致）。一行逻辑改动，括号平衡自查通过。
- **本地待推 2 笔**（1 代码 73d5f9d + 1 记录）。等目目说「推」。
- 备注：为什么 09-09 没暴露——当时面板按 tag 比对能命中（数据里 tag 就是中文），后来批量分配旁白标签改成 tag=narration+tagName=旁白 的双字段写法后此面板没跟着改。
- **14:01 目目拍板改精**：只按 `tagName=="旁白"` 筛（旁白的 tag id 均为英文，比 tag 无意义），撤掉 tag||tagName 双字段写法（0c7e6fe）。**本地待推 4 笔**（2 代码 + 2 记录）。

## 14:08 快捷栏「更换发音人」按分类分流（6951294，未推送）
- 目目给大集合分组数据指出：还有其他分类要处理——**本地音效（tagName 带序号，如"本地音效1"，tagData.audioName 是音效正则）按角色那种（绑定式）处理**；**其他分类（【】括号发音人/「」括号发音人/『对话旁白』/女/男/（男/少年））按旁白那种处理**。
- 实现（LogQuickPanel）：
  1. 新增 `bindingKey`：多角色日志=entry.roleName；否则 tagName 匹配 `本地音效\d*` → 用 tagName 当槽位名。`isBindingMode = bindingKey.isNotBlank()`；读绑（boundVoice 初始化）、改绑（rebind）、toast 全部从 entry.roleName 换成 bindingKey——本地音效槽位与角色共用 characterRecords.json 同一机制。
  2. 非绑定分支从硬编码「旁白」推广为 **本配置项 tagName**：候选=全池同 tagName 配置（含 voice 非空），选一个写本配置项 voice；`remember(entity.id, currentTagName)`；空分类提示改「「xx」分类没有可用的配置项」。
- 效果：大集合里点「本地音效1」的日志行 → 绑定式改绑槽位；点对话/括号类日志行 → 列出同分类全部候选换声。
- 改动 1 文件 +19/−13；括号平衡自查通过。**本地待推 7 笔**（3 代码 + 4 记录）。等目目说「推」。

## 14:23 绑定模式候选也统一只按 tagName（026997b，未推送）
- 目目问：多角色绑定模式是不是 tag/tagName 两种混着筛？→ 查证：**原本只按 tag id 筛**（fayinren.json 池 ∩ 启用配置的 tag 字段），不是混用——但池子文件、`voiceCategoryOf`（参数名就叫 tagName，按中文显示名设计）、角标全是显示名口径，唯独筛选用 id，正是旁白那次翻车的同类隐患。
- 目目拍板：**用 tagName 筛就足够了**。改动：`enabledTags`→`enabledTagNames`（收 tagName）；`enabledConfigEntityByTag`→**`enabledConfigEntityByTagName`**（比对 tagName，全 5 处调用点更名）；`boundVoice` 兜底 `?: tagName`。**LogQuickPanel 全文件 `speechRule.tag` 引用清零**——面板从此只活在显示名世界。
- 注意：characterRecords.json 的 `voice` 字段、rebind 写入值，随之全部为 tagName——**依赖规则 JS（mingwuyan）运行时按 tagName 匹配配置**，装机必验「换绑后真的换声」。

## 14:44 对话类（duihua）候选范围两轮修正（cf949ec + f524eec，未推送）
- 目目指出 duihua 特殊：**tagName 变化多样**（男/少年、男/男青年…，规则里 tagName=角色关键词+（性别/年龄），条条不同）→ 按 tagName 细分对 duihua 无效，**判定依据=多角色朗读**（规则 mingwuyan 的 tags 表：duihua→对话、duihuaA→男、duihuaB→女；规则源码在仓库 `tts配套文件/多角色朗读2.87…同步显示名.js`）。
- 第一版按 startsWith("duihua") 大类互通（cf949ec）→ 目目纠正：**仅 tag=="duihua"** 的配置互为候选（f524eec）——这批配置 genderAge 各不相同（男/少年、女/女青年…十来种）、tagName 条条不同，运行时随机挑一个；**duihuaA(男)/duihuaB(女) 是另外的类，不掺入**。空列表提示区分「对话分类」。
- 教训：目目指路"看仓库里的规则 JS（tts配套文件/）"比猜机制快——以后遇到标签机制问题先读规则源码。
- **本地待推 9 笔**（4 代码 + 5 记录）。等目目说「推」。

## 15:01 绑定匹配改任一命中 + duihua 备用移交 duihuaA（d2d45dd，未推送）
- 目目补充机制认知：**tag 与 tagName 是绑定关系**（tag 底层不变、tagName 是显示名；大部分相同，少数英文id+中文名如旁白，duihua 这种 tagName 多变）→ 拍板绑定匹配改 **tag/tagName 任一命中**。我的判断：并集匹配不会误伤（id 英文/拼音、tagName 中文，无撞车面），兼容池子/记录存 id 或显示名的历史差异——采纳。
- 改动①（LogQuickPanel）：`enabledTagKeys`=启用配置 tag+tagName 并集；`enabledConfigEntityByTagName`→**`enabledConfigEntityByKey`**（`rule.tag==key || rule.tagName==key`），5 处调用点跟改。`voiceCategoryOf` 未动（池里若混入英文 id 会落「全部」分类，可接受）。
- 改动②（TtsRepository，duihua 备用移交）：性别兜底映射「性别未知→duihua」改投 **duihuaA**（tagName=男）；并留兼容尾巴——找不到 duihuaA 配置时**回落 duihua**，以前只用 duihua 的老用户不断声。AbstractMixSynthesizer:219 的错误日志分类集合不动（老数据仍识别）。
- 规则 JS（mingwuyan）未动：duihua 标签仍在 tags 表里声明，老用户数据照常工作。
- **本地待推 10 笔**（5 代码 + 5 记录）。等目目说「推」。

## 15:22 匹配口径终版：一律按底层 voice（f8025da，未推送）
- 目目指正："按标签查配置 tag/tagName 任一命中"不对——**应该看 voice，voice 更权威更底层**；并指路先读《技术文档/字段关联架构.md》。
- 文档实锤（§6.1 方案B）：JS 返回的 TextWithTag.tag 在方案B场景下放的是 **voice 值**，匹配层第3步就是 `voice == txtWithTag.tag`；即池子（fayinren.json）/characterRecords 里 travel 的绑定值是 **voice**（每配置唯一）。tag=逻辑标签（多配置可共用）、tagName=显示名——两者都不该拿来当绑定匹配键。
- 改动（LogQuickPanel）：`enabledConfigEntityByKey`→**`enabledConfigEntityByVoice`**（比对 `source.voice`）；池子过滤改启用配置 **voice 集合**（enabledVoices）；boundVoice 兜底改**本配置项 voice**；`narrationEntityByVoice` 本就是 voice 口径（先前按 voice 写 voice 的链路全对齐了）。
- 撤 TtsRepository 的 duihua 兼容回落（目目：不用留）——中性备用只投 duihuaA，没有就没有。
- **本地面板绑定匹配演进记录**：tag id → tagName → tag||tagName → **voice（终版）**。教训：改匹配口径前先读《字段关联架构.md》，它就是为这种字段混淆写的。
- **本地待推 11 笔**（6 代码 + 5 记录）。等目目说「推」。

## 15:33 匹配口径二次反转，终版=**tag（id）**（aba3e03，未推送）
- 目目再指正：①分配标签靠的是 **tagName**（角色管理/批量分配的 UI 操作都落在显示名上）；②**一个 tagName/tag 绑定多个配置（多个 voice），随时切换**——绑定键是"标签（槽位）"不是某个 voice。
- **规则 JS 实锤**：fayinren.json = `availableVoices` 的键 = **tag id**；characterRecords 的 `voice` 字段也是 tag id（JS 里 `tags[voiceTag]=显示名` 仅用于回显）；运行时匹配层第2步 `speechRule.tag == 分配tag`，**同 tag 内多配置随机**。→ 绑定域从头到尾都是 tag id，voice 是每条配置自己的底层属性、不是绑定键（f8025da 的 voice 口径是过度修正，已回退）。
- **终版实现**（LogQuickPanel）：候选/绑定分组键 = **tag（id）**——非绑定分支按 `rule.tag == 本配置项.tag` 列同标签候选（旁白/duihua/括号/本地音效天然归组，**isDialogueTag 特殊分支删除**）；绑定分支池子= fayinren ∩ 启用 tag 集合（tag+tagName 并集）；查配置 = `enabledConfigEntityByTagKey`（tag 命中为主、tagName 兼容）；boundVoice 兜底 = 本配置项 tag id。空提示改「「xx」标签下没有可用的配置项」。
- **本次教训（重要）**：面板匹配口径被我来回改了四版（tag→tagName→tag||tagName→voice→tag终版），根因是我没先读规则 JS 和《字段关联架构》就动手。**以后凡是涉及 tag/tagName/voice 的匹配问题，第一步先读 `tts配套文件/多角色朗读*.js` 的 tags 表与《字段关联架构.md》匹配层**。
- **本地待推 18 笔**（7 代码 + 11 记录）。等目目说「推」。
