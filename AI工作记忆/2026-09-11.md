# 2026-09-11（深夜）音频参数 UI 三处统一收官（52d492c，未推送）

## 用户拍板过程
- 编辑页三项行之前被上次 AI 记成"纯文字不加框最轻"，用户澄清：没要求过不加框，要的是**整体和谐**——跟试听融合、跟弹窗统一 → 定方案A：编辑页改用与弹窗/日志同款软槽。
- "收起态怎么知道能展开" → 定**永不全收起**（默认展开语速，浮块常驻即可点性示范，与弹窗"永远有一维选中"行为一致）。
- 追加三条：作为备用问号文案太啰嗦要精简；心声混响、内心独白加问号解说；内心独白取消勾选要还原原标签（之前是清空）。

## 改动清单（commit 52d492c，9 文件，等用户说「推」）
1. `LabelSlider`（lib-compose）：轨道回胶囊画法 10dp（圆角胶囊+尾端小圆点+竖条 thumb 4×22），替换 3.5dp 细线；默认值不动，仅 LayerSlider 传参生效
2. `SoftSegmentedTextToggle`：选中项浮起 primaryContainer 胶囊 + onPrimaryContainer 粗体
3. `AudioParamsDimRows`：纯文字行→同款软槽（16sp 带终值、默认展开语速、永不全收起、顶距 12dp、展开区撤底框）；删了 `if (dim >= 0)` 层，注意括号已配平、imports 已清
4. `AudioParamsDimensionSection`：LayerSlider 传 10dp/4×22；软槽顶距 8dp；注释更新
5. `LogQuickPanel`：父级「更换发音人/音频参数」16sp→14sp（父子区分只靠形态）
6. strings（values + values-zh）：发音人→**本项**；备用文案精简；新增 systts_reverb_help*/systts_inner_thought_help*
7. `BasicInfoEditScreen`：复选框 -15→-10dp、文字 -8dp、心声混响加问号、作为备用问号撤 +12dp
8. `SpeechRuleEditScreen`：内心独白勾选时记原标签（remember(systts.id)）、取消还原；同款方块/文字/问号调整
9. 记忆已更新：MEMORY.md 音频参数定案改写 + 新增「编辑页问答机制补充」节

## 待办
- 等用户说「推」→ push → CI 验证（本机不编译）
- 内心独白还原的跨会话局限已告知：勾选→保存→重开再取消，原标签无从得知，回落清空

# 2026-09-11（上午）边距/间距审读收官（60ccefc + 7a676f3 + f752e9e + 0177c22，均未推送）

## 用户拍板与裁定
- 英文层名：用户指认 `audio_params_tag_config` 该译 **Config**（语义=配置层，对应 plugin/global），Voice→Item（60ccefc）→Item→Config（7a676f3 改正）。
- 日志面板 ▶ 试听**维持硬编码**「你好，这是试听语音。」，不接试听文本框（撤销凌晨的 auditionText 同源接线）。
- 弹窗边距（AskUserQuestion 拍板）：日志面板左右统一 16dp（内容 Column 整体 +4dp，撤音频参数区单独 4dp）；终值行上下各 4dp 消 2dp 差。
- 软槽→第一行滑杆补 top=4dp（唯一不齐处，编辑页同位置本就 4dp；共用组件一改三处同步，f752e9e）。
- 滑杆行 **撤 labelMinWidth=44dp**（0177c22）：定宽是 3 字「发音人」时代遗留，改「本项」后三行 2 字+数值 4 字符天然等宽，撤掉省 16dp 死空间。
- 发音人行重排（0177c22）：「当前发音人」小标签独占一行，**▶ 键与发音人名同一行**（用户明确要同行；否掉了圆形填充键方案，保持文字键）；名字加 TextOverflow.Ellipsis（原来 maxLines=1 无 overflow 会硬裁）。

## 教训
- 审读示意图里省略 −/＋ 按钮画成文字贴轨道，被用户误读为"贴那么近"——间距图必须画全真实元素链，不可简化掉中间件。
- 滑杆行真实横向链：文字→16(死,已撤)+8(定稿)→[48dp触摸区/22dp图标,左右各约13dp内缩]→轨道。

# 2026-09-11（上午）推送与打包收官（0b36ac8 + tag 1.26.09110951，已发布）

- 首轮 CI（34551857406）失败：`SoftSegmentedTextToggle.kt` / `AudioParamsDimensionSection.kt` 漏 `import androidx.compose.foundation.layout.padding`（浮起胶囊 padding 与间距改动引入，本地不编译故 CI 才暴露）。`0b36ac8` 补两处 import，重推。
- 二轮 CI（34552108275）success；代打 tag **1.26.09110951**（用户明示"验证通过就直接打包"），Build Release（34552333809）success，Release 资产 `TTS-Server-v1.26.09110951.apk` 已发布。
- 本轮包内容 = 52d492c + 60ccefc + 7a676f3 + f752e9e + 0177c22 + 0b36ac8（09-11 全部 UI 定案）。

# 2026-09-11（上午二轮）装机反馈打磨（227636a，未推送）

- 编辑页「朗读全部/标签」M3 SegmentedButton 16sp→14sp（当年升 16 是对齐日志面板父级，日志已降，此处成全 app 唯一大号分段）。
- 「内心独白」更名「**心声标签**」（标签字段勾选态显示值仍是「心声(内心独白)」未动）。
- 复选框行三处统一定稿：文字 offset -12dp（距方块约 3dp）+ 问号 IconButton size(32dp)（距文字约 4dp）——用户原话"不要留空"。三行=心声标签(SpeechRuleEditScreen)/心声混响+作为备用(BasicInfoEditScreen)。
- 弹窗顶部三行（当前发音人/名字/终值）=紧密信息：终值行上下 4→2dp（弹窗+日志面板两处）。
- 共用维度区（AudioParamsDimensionSection，弹窗/日志自动同步）：软槽→滑杆 top 4→8dp；滑杆区 start 缩进 8dp；LayerSlider 标签 14→12sp（含数值行）。编辑页 AudioParamsDimRows 展开区手动同步（top8/start8）。
- 教训续：加 size/offset 类改动先查 import（本环境不编译，import 漏只能 CI 抓）——本轮 SpeechRuleEditScreen/BasicInfoEditScreen 均补 foundation.layout.size。

# 2026-09-11（上午三轮）字号统一收尾（d61dcaa，未推送）

- 编辑页音频参数软槽 16sp→14sp（用户"要跟音频参数弹窗一模一样"），三处软槽彻底统一 14sp。
- **弹窗标题全局 22sp→16sp**：AppDialog 默认 titleLarge→titleMedium（全 app 弹窗一处降）；显式写死 titleLarge 的三处弹窗标题同步（AppUpdateDialog×2 / BatchTagDialog / GroupEditContentDialog）；ListManagerScreen:1436「转为子分组」是页内卡片标题非弹窗，未动。
- 注意：弹窗标题 16sp 与内容默认（LocalTextStyle titleMedium）同字号，层级靠居中/左对齐区分——若用户后续嫌标题不显眼可回 titleLarge 或折中 18sp。

# 2026-09-11（上午四轮）弹窗标题终裁与备份弹窗归队（0e5edb8，未推送）

- **弹窗标题 16→18sp 终裁**：用户装 16sp 后嫌不显眼，取折中档 18sp。AppDialog 默认 `titleMedium.copy(18.sp)`；昨日显式写死 titleMedium 的三处（AppUpdateDialog×2 / BatchTagDialog / GroupEditContentDialog）**撤显式样式回落默认**——教训：改共用默认值时，之前为应急写死的显式样式要同步回收，否则锁死旧值造成不一致。
- **标题→内容 8dp**：AppDialog 标题与内容 Box 原 **0dp 贴死**（Column 无 spacedBy），是全弹窗共性缺陷；备份弹窗因顶部即分段控件最明显。共用组件 content Box 补 `padding(top = 8.dp)` 一处生效。
- **备份弹窗**「完整备份/分享备份」描边胶囊（SegmentedTextToggle，宽度随文字+居中）→ **SoftSegmentedTextToggle 软槽**（浅底槽+浮起胶囊，等分撑满），与音频参数弹窗语速/音量/音高归队。SegmentedTextToggle 现仅剩日志面板父级一处使用。
- 用户工作模式确认：「你先看是否合理，不要全盘认同」——评估类需求要先给独立判断再动手，本次三条均合理但修正了第2条的归因（不是备份弹窗特有，是全弹窗共性）。

# 2026-09-11（上午五轮）滑条全 app 统一终裁（fa3dfa0，未推送）

- **规则反转（重要）**：09-10「改尺寸只能走 LayerSlider 参数、禁止改 LabelSlider 默认值」被用户 09-11 终裁推翻——「其他地方用到滑条的也全统一（设置/背景音乐等）」。LabelSlider 默认值即统一值：胶囊轨道 10dp、thumb 4×22、±48dp/22dp 图标、标签 12sp（原默认 13sp/32/20/3dp/3×20）。LayerSlider 已撤尺寸传参只留文案/范围/步进。受影响：SettingsWidgets、BasicAudioParamsDialog（全局弹窗）、BatchAudioParamsDialog、FloatSlider/IntSlider 包装、InternalPlayerDialog、BgmConfigUI。
- 备份弹窗标题固定「备份」（原随模式切完整备份/分享备份，与正下方模式分段文字重复）；modeTitleRes 已删。
- 应用按钮 ● 绿点脏标记撤（DimensionSection + DimRows 两处）；isDirty 流水保留不用于渲染（入参仍在，注释已说明）。
- 插件音频参数弹窗 AlertDialog→AppDialog 外壳统一 + 内容水平 4dp；滑条经 FloatSlider 随默认值自动统一。
- 本地待推提交累计：227636a / d61dcaa / 0e5edb8 / fa3dfa0（四轮 UI 打磨），等用户说「推」。

# 2026-09-11（上午六轮）染绿归因纠偏（用户指认，纯问答无代码改动）

- 用户指认「一级分组菜单弹窗/右键菜单也不染色、应该都是 MD3」——**核实后用户对**。染绿主犯是 AppDialog 自己的 `tonalElevation=8dp`（git：5575f6c 用户嫌"弹窗过深"、fa069a9 用户嫌"遗留绿"，最终定纯白底+0海拔+8dp阴影）；菜单/原生 AlertDialog 默认仅 3dp，绿色混色肉眼无感。
- **勿再引用"原生壳在豆绿主题下发绿"作为换 AppDialog 的理由**——对 3dp 原生弹窗不成立。换壳的真理由只有：样式可控 + 全 app 一致（标题/边距/按钮统一改一处生效）。

# 2026-09-11（上午七轮）音频参数弹窗草稿制统一（05894d8，已推送，CI 34559799105 盯守中）

- **用户拍板方案 A**：全局/插件音频参数弹窗统一草稿制。全局弹窗原滑条直接绑定 DataSaver（拖动即写库、无法反悔、服务延迟到关闭才通知）→ 改本地草稿 + 点「应用」落库 + notifyUpdateConfig；resetValue=1f 全 1.0 置灰重置键。
- 按钮排布统一：取消（左）｜ 重置 · 应用（右），fillMaxWidth Row 撑两侧（AppDialog buttons 是 End 对齐 FlowRow，靠 weight Spacer 分两侧）；插件弹窗「确定」→「应用」（audio_params_apply），全 app 音频参数只剩"应用生效"一种心智。
- **弹窗壳路线讨论结论（重要背景）**：用户提出"用人家的默认说不定就不用改了，MD3 标准应该和谐"——认真对待。两套壳现状 = 23 文件原生 AlertDialog vs 29 文件 AppDialog。建议用户装机后对比插件音频参数弹窗（AppDialog 壳）与设置页原生弹窗再定路线；两条路都可行，2 号路（全回 MD3）需主题层 surfaceTint 中性化配套。**未做最终决定，勿擅自启动 23 文件迁移**。
- 待推批次：227636a/d61dcaa/0e5edb8/fa3dfa0/05894d8 全部已随本次推送上远端。
- CI 34559799105 **success**（05894d8 草稿制统一 + 此前四轮 UI 打磨全部验证通过）。本轮只验证未打包（用户未要求出包）；弹窗壳路线（AppDialog vs MD3）待用户装机对比后定。

# 2026-09-11（中午）复选框行根治（9c66a0a，未推送）

- 用户装机截图实锤：「□作为备用」文字被方块压住——负偏移叠补的锅：M3 Checkbox 48dp 触摸区画 18dp 方块（两侧各空 15dp），文字 offset(-12dp) 回拉，叠加行级 offset(-10dp)，魔法数字互相打架。**每轮加新数字抵旧数字 = 反复出问题的根因**（对应硬约束"重复反馈零容忍"的教训样本）。
- **根治模式（今后复选框行标准）**：撤全部 offset，复选框触摸区 `Modifier.size(32.dp)`（与问号 IconButton 同款）→ 方块两侧自然剩 7dp，文字自然位置即紧凑，结构上不可能重叠。三行（心声标签/心声混响/作为备用）统一。offset import 已清。
- MD3 弹窗问答补充结论（用户在装好的包上对比后说"两种都行、以前 MD3 也不难看"）：MD3 有官方规范自动保鲜、主题级（colorScheme/typography）也可一处改全局，但按钮排布/标题居中等布局细节无口子。**路线未定**，用户倾向"零维护"，等待其表态后可启动 29 文件回迁 MD3 清单（逻辑层改动如草稿制原样保留）。

# 2026-09-11（中午）MD3 路线启动（9c979c3，已推送，CI 34562109527 盯守中）

- **用户终裁**：复选框修复先推作备份，然后"全改为 MD3 我去看效果，合适了就不回退"——MD3 路线正式上机验证。
- **实现策略（一处改全局）**：AppDialog 签名/调用方零改动，内部 BasicAlertDialog+自绘 Surface → material3 原生 AlertDialog。29 个调用方全部变 MD3 观感；回退 = git revert 9c979c3 一个提交。要点：content 包 Box 保 BoxScope 签名；buttons 塞 confirmButton 槽外保留 FlowRow 换行；dialogContentPadding 默认 0（MD3 自带 24dp，AuditionDialog 的自定义值会叠加在 MD3 内边距上，观感偏松属预期）；白底零海拔定制废弃；标题回 headlineSmall 24sp（MD3 规范，勿再按 18sp 记）。
- 待验证风险：内容超高且无内滚的弹窗在 MD3 text 槽不会自动滚动（现有长内容弹窗均自带 verticalScroll，理论上安全）；CI 编译过与否见 run 34562109527。
- 用户"看效果"需要出包：CI 过后需问是否代打 tag。
- CI 收官：34562287508 **success**（6431ed7）。MD3 全弹窗化（AppDialog 内部换原生 AlertDialog）+ 复选框根治全部验证通过，远端 head = 6431ed7。等用户看效果定夺（合适不回退 / revert 9c979c3+6431ed7 回自绘壳）；出包需用户确认后代打 tag。

# 2026-09-11（午后）MD3 路线定案（082af29，已推送，CI 34563572740 盯守中）

- **用户装机反馈**：MD3 版"没看出来区别"→ 终裁"直接彻底按 md3 来吧"。MD3 路线**正式定案，不再摇摆**。
- 彻底化收尾（082af29）：①撤 `dialogContentPadding` 参数（MD3 自带 24dp 规范内边距，AuditionDialog 的自定义值一并撤+PaddingValues import 清理）②撤正文 LocalTextStyle 覆盖（MD3 text 槽自带 bodyMedium）③删自绘 AppDialogFlowRow + ButtonsMain/CrossAxisSpacing 常量（MD3 按钮行官方不换行）。AppDialog 净减 ~120 行。
- **维护铁律（新）**：AppDialog 定位=纯签名兼容垫片，**永不加自定义样式**；要改弹窗观感→改主题（colorScheme/typography）或等 MD3 官方演进，勿再往组件里堆魔法数字。历史教训闭环：09-03/04 染绿自绘壳 → 09-11 彻底回归标准。
- "没看出区别"的解读：自绘壳本就调到接近标准，两套观感本就趋同——这正是"MD3 零维护"说服力所在。
- CI 34563572740 **success**（082af29 彻底 MD3 化验证通过）。远端 head=082af29，MD3 路线定案收官，等用户说打包再代打 tag。

# 2026-09-11（13:07）状态存档 + 用户新指令：做"完全 MD3 版"
- **存档时状态**：工作区干净，本地/远端 head 均为 082af29（AppDialog 彻底 MD3 化），CI 34563572740 success，用户已自打包 1.26.09111252（指向 082af29）并安装。无未提交改动、无待推内容。
- **用户新裁定（13:07）**：装包后感觉"还是跟之前设的一样"，认为**自定义的（弹窗内容组件）并不好，还是按官方来**——要求再改一个"完全 MD3 版"，即把 LabelSlider 自绘轨道/软槽 SoftSegmentedTextToggle/32dp 复选框/手调字号等内容组件也回归官方默认。**硬约束提醒：UI 改动先提案后动手** → 本轮先出方案+对比示意图，AskUserQuestion 确认范围（视觉回归但保留功能结构 vs 连 +/- 键也撤）和分支策略（直接改主分支 vs 开分支做对比版）后再动代码。
- 功能结构（三层滑杆、草稿制、试听链、布局间距）不在"MD3 与否"范畴，默认保留。

## 13:32 完全 MD3 版已提交（77e955b，未推送）
- 范围经 AskUserQuestion 拍板：**保留 ± 加减键**（功能结构）、**直接改主分支**。
- 改动 7 文件：LabelSlider 撤自绘轨道/thumb（trackHeight/thumbSize 参数删除，iconSize 22→24dp）；SoftSegmentedTextToggle 内部换官方 SingleChoiceSegmentedButtonRow+SegmentedButton（函数名/API 不变，三处调用点自动生效，撤 labelFontSize/containerHeight）；AudioParamsDimRows 撤 14.sp 传参；BasicInfoEditScreen/SpeechRuleEditScreen 复选框+问号 IconButton 撤 32dp 回官方 48dp；LayerSlider/LogQuickPanel 注释同步。
- 未动：三层滑杆结构/草稿制/试听链/布局间距/豆绿主题/父级 SegmentedTextToggle（描边胶囊）。
- 已 grep 验证无残留引用（trackHeight/thumbSize/labelFontSize/containerHeight/size(32.dp) 在目标文件全清）。
- 下一步：等用户说「推」→ push → CI（gh run watch）→ 用户对比装包；若满意此形态，记忆里 09-11 的"胶囊轨道/软槽/32dp"旧定案条目需改写为"已被完全 MD3 版推翻"。

## 13:47 父级分段也回归官方（5a9d7ff，未推送，在 77e955b 之上）
- 用户确认把最后一处组件级差距收掉：日志面板顶部「更换发音人/音频参数」自绘描边胶囊 SegmentedTextToggle → 换 SoftSegmentedTextToggle（新增 equalWidth=false=宽度随文字；官方 SegmentedButton 不加 weight 本就随内容收缩，保住"不均分"行为）。
- SoftSegmentedTextToggle 加 equalWidth 参数（默认 true，既有三处调用不受影响）；SegmentedTextToggle 自此无调用方，标废弃留文件占位（勿 git rm）；LogQuickPanel 补 SoftSegmentedTextToggle import、撤 sp import。
- **至此"完全 MD3 版"组件级收口**：弹窗壳/滑条/分段（父子同源）/复选框/问号按钮全部官方 MD3 默认。未统一项仅三类（均已向用户说明）：①布局间距（MD3 无 spacing token，4dp 网格合规，无官方默认可回）②± 加减键（功能结构，官方 Slider 无此物，用户拍板保留）③豆绿主题（Material Theme Builder 种子色生成，本身即 MD3 合规）。终值行 2dp vs 4dp 差异仍未拍板。
- 环境坑：本轮 Bash PATH 抽风（cat/grep/tail 全找不到），heredoc 追加静默失败、exit 0 伪装成功——**追加记忆后必须 Grep 验证**；git commit 可用多 `-m` 参数绕过 heredoc。
- 等用户说「推」→ push → CI → 对比装包（对比基线：1.26.09111252）。

## 13:55 推送 + CI 完成（用户说「推」）
- `git push origin mumu_tts_github` 成功：`082af29..5a9d7ff`（`77e955b`+`5a9d7ff` 两笔）。
- CI 编译验证 run `34567889150`（push 触发，test.yml paths 白名单命中 lib-compose/app/src）→ **success**，build 2m58s，Compile Check ✓；仅 Node.js 20 deprecation 提醒（无关）。
- 未打 tag、未触发打包（用户只说「推」，按分工 AI 不打 tag）；等用户自装包对比。对比基线 1.26.09111252（指向 082af29）。

## 14:35 剩余弹窗 MD3 化（9e51e16，未推送）
- 用户装包反馈"大部分没问题"但发现仍有弹窗非 MD3，举例"配置项→移动到子分组"。定位：该入口实际开的是 **GroupTreePickerDialog**（统一分组树选择器，非同名 MoveToSubGroupDialog），官方 AlertDialog 叠了白底+16dp 圆角+0.9 宽（白底注释还写着"与 AppDialog 白底统一"的过时说法）。
- 全 app 盘点后共改 8 处弹窗：GroupTreePickerDialog / BatchTagDialog / GroupEditContentDialog（白底+小圆角+自定宽全撤）；ListManagerScreen 4 处（platform width+0.92 宽撤，其中"多选转为子分组"是 Dialog+Surface 自绘 M2 壳，整体换官方 AlertDialog）；PluginManagerScreen 2 处白底撤。
- **MoveToSubGroupDialog 已无调用方**（列表"移动"第 6 项改用分组树选择器后），文件留占位未删。
- 不动的：TtsLogScreen fillMaxWidth(0.95f) 是 SearchBar 非弹窗；NavTopAppBar surface 是官方顶栏默认；TextFieldDialog 内部本来就是干净官方 AlertDialog。
- 小坑：编辑 ListManagerScreen import 时误把 `import androidx.compose.ui.window.Dialog` 替换成 LocalView（无使用），已复查删除；教训=**删 import 的 Edit 里 new_string 不能顺手换成别的 import，先 Grep 确认新 import 有无使用**。
- 等用户说「推」→ push → CI → 装包对比。

## 15:05 推送 9e51e16 → CI 失败一次 → 修复 435afc9 → CI success
- **CI 34572242334 failure**：ListManagerScreen 1426 "None of the following candidates is applicable"+连锁 @Composable 错——根因=「多选转为子分组」弹窗换官方 AlertDialog 时**漏了必选的 confirmButton 槽**（此弹窗只有取消键，原自绘壳无确认概念）。修复=取消键放 confirmButton 槽（435afc9）。
- **教训（勿再犯）**：material3.AlertDialog 的 confirmButton **无默认值必填**；自绘壳转官方壳时，即使无正向确认也要往 confirmButton 槽放东西（空槽或取消键）。dismissButton 才是可选。
- **CI 34572956811 success**（435afc9）。未打 tag。
- **全 app MD3 终审（Explore 代理扫 + 人工验证，只读）**，报告已给用户等拍板：
  - ✅ 合规：BottomSheet×8 全官方 ModalBottomSheet；菜单/Switch/Snackbar/Toast；无 XML 残留；RestoreDialog/TextFieldDialog/AppDialog/ErrorDialog/AppSpinner 薄封装内部官方；硬编码色仅滚动条 0xFF888888 与 CodeFoldingOverlay gutter 橙蓝（功能层小元素）。
  - ⚠️ 待拍板：①**LoadingDialog**（lib-compose/widgets，全局加载，Dialog+Surface 4dp 自绘壳+自绘渐变扫掠转圈 600ms）→ 建议换官方 AlertDialog 壳+官方不确定态 CircularProgressIndicator；②**混元太极页 COutBtn+compactTypography**（deepseekproxy/MainActivity.kt，自绘按钮是为保长按手势——官方 OutlinedButton clickable 会抢占 detectTapGestures 长按；整页字号 -2sp）→ 有功能理由建议保留；③ForwarderWebDialog（全屏 Dialog+Scaffold 内部全官方，用户不开转发器）→ 不动；④死代码 ButtonToggleGroup/LoadingAnimation 零调用占位。
  - ListImportBottomSheet 等 8 处 BottomSheet 已确认官方。

## 15:15 LoadingDialog MD3 化（7634e23，未推送，用户拍板改①）
- lib-compose/widgets/LoadingDialog.kt 整体重写：Dialog+Surface(4dp) 自绘壳 → 官方 AlertDialog；自绘渐变扫掠转圈（ProgressIndicatorLoading，仅本文件用）→ 官方不确定态 CircularProgressIndicator；LinearProgressIndicator 确定态本已官方保留。confirmButton 必选槽传空（加载弹窗无按钮）。签名不变，9 处调用方自动生效。+27/−69。
- ②混元太极页（长按手势理由）③转发器浮层 用户未要求改，维持现状。等用户说「推」→ CI → 装包。

## 15:25 推送 7634e23 + MD3 规范立约
- push 435afc9..7634e23 成功，CI run 34574206911 后台盯（ydL3gk）。
- **MD3 全家族 12 块覆盖表已答用户**（弹窗/BottomSheet/滑条/分段/选择/进度/Chip/TabRow/SearchBar/按钮FAB/菜单/导航Pager，全 ✅；未使用：DatePicker/TimePicker/NavigationRail/Badge/Carousel）。
- **用户立规：以后新增 UI 必须符合 MD3 标准，避免反复回调** → 新建《E:\TTS\技术文档\MD3 UI 规范约定.md》（七条：官方组件/typography token/colorScheme 槽位/4dp 网格/shapes token/48dp 触摸/提交前 grep 自查 + 历史包袱备忘），并写入 MEMORY.md 硬约束第 10 条。
- 字号 vs 间距的定性答复：**MD3 有 Typography 标尺（15 token，字号有官方标准）**；**间距无 token**（M3 系统无 spacing 槽，只有 4dp 基线网格软约定），故间距约定为"取 4 的倍数 + 常用档位"。
- **CI 34574206911 success**（7634e23）。未打 tag，等用户自装包对比。今日 MD3 收官链：77e955b → 5a9d7ff → 9e51e16 → 435afc9 → 7634e23，全部已推送且 CI 绿。

## 15:48 字号专项收口（f92b460，未推送，用户拍板 A 方案）+ 补查三项
- **全 app 硬编码 sp 扫描**：26 处命中。A 方案落地 11 处违规全回 token（f92b460，10 文件 +24/−36）：GroupItem 18sp→titleMedium；SubGroupHeader 17/16/16→titleMedium/titleSmall/bodyLarge；SettingsWidgets/replace Item/PluginManagerScreen×2 的 15sp→bodyMedium；list Item HtmlText 13sp→bodySmall（主次差改用颜色 onBackground vs onSurfaceVariant，因 13sp 无对应 token 且 09-10 用户嫌 14 大）；AuditionDialog chip 撤 12sp；LabelSlider 撤 labelFontSize 参数回 bodySmall（无调用方传参，已 grep 确认）；SpeechRuleEditScreen×2 冗余 copy 清理。清 8 文件失活 sp import。
- **豁免**：Type.kt（Typography 定义本体）、deepseekproxy 12sp（特例许可）、SegmentedTextToggle（死代码）。
- **补查项结论**（回答"还有无遗漏检查项"）：
  1. Color 常量散用（Black/White/Red 等）→ ✅ 干净，全是 Color.Transparent 官方惯用法（透明化官方组件自带色）
  2. FontWeight 散用 → ~17 处 Bold：分两类，选中态 if(selected) Bold（状态表达，建议保留）；静态标题 bodyMedium+Bold（~7 处，理想应换 titleSmall/titleMedium token，待用户决定是否收）
  3. 网格外 dp（2/6/10dp）→ ~30 处存量（日志面板/列表/分组树的紧凑行距），全改视觉明显变松，**建议保留现状**（紧凑是打磨期刻意设计）；2dp 终值行遗留项同性质
## 16:13 静态 Bold 收口（e0baff6，已推送，用户拍板：收①静态加粗，②网格外间距保留）
- 全量 17 处 FontWeight.Bold 分类：**保留 4 处选中态**（ListSortSettingsDialog:73 if(selected)、TagSwitchDialog:155/192 if(isCurrent)、PluginTtsUI:378 if(isSelected)）+ **1 处官方 Medium**（list/Item.kt:398 卡片标签 FontWeight.Medium=官方 500 字重，非 Bold 滥用，保留）。
- **撤 12 处静态 Bold**：ConfigDeleteDialog×2（标题 titleMedium+按钮"删除"）、TagDataClearConfirmDialog 按钮、SectionCard titleSmall、list/Item 配置项标题 titleMedium+"备用"labelMedium、PluginTtsUI 插件角标、BgmConfigUI/ThemeSelectionDialog error 正文、AboutDialog GitHub 链接、TagSwitchDialog×2"当前"角标、ReplaceRuleEditScreen 正则键盘按钮。全部回 typography token 自带字重（title/label 系官方即 Medium 500，撤后视觉从 700→500）。
- 清 7 文件失活 FontWeight import（AboutDialog/ThemeSelectionDialog/ConfigDeleteDialog/ReplaceRuleEditScreen/BgmConfigUI/TagDataClearConfirmDialog/SectionCard）；Item.kt import 保留（Medium 在用），注释同步。
- ②网格外间距（~30 处 2/6/10dp 紧凑行距）按用户裁定保留，勿再提议全网格化。

## 16:17 收官补查四个新维度（用户要求零遗漏）+ CI 通过
- **CI 34578662751 success**（f92b460+e0baff6 已推送）。今日 MD3 收尾链全绿：77e955b→5a9d7ff→9e51e16→435afc9→7634e23→f92b460→e0baff6。
- 补查结论：①RoundedCornerShape 硬编码 7 处（GroupItem 4dp 标签、ListManagerScreen×2 8dp、LogScreen 6dp、LogQuickPanel×2 8dp、SegmentedTextToggle 死代码）→ 对应 shapes token extraSmall(4)/small(8)，可等值换 token；②elevation 硬编码 2 处（CodeEditorScreen 4dp 搜索栏容器、ListManagerScreen 3dp 批量操作条）→ Surface 容器级别微调，属灰色地带；③全干净项：LocalTextStyle provides 8 处全走 token、alpha 手动禁用态 0 处、Divider 仅 1dp 显式默认值、TextField 全官方 OutlinedTextField、darkColorScheme 10 套齐全、ripple 用官方 M3 API；④两处已核现场可接受：SettingsSearch:127 OutlinedTextFieldDefaults.colors 边框全 Transparent 做"填充式搜索框"（SearchBar 平替形态）、SettingsWidgets:84 titleSmall.copy(color=primary) 仅调色。

## 16:42 圆角 token 化收官（96e76d2，已推送，CI 34580067226 success）——MD3 全链完结
- **96e76d2**：6 处 RoundedCornerShape 换官方 shapes token——GroupItem 4dp→extraSmall、ListManagerScreen 8dp×2→small、LogQuickPanel 8dp×2→small、LogScreen 6dp→extraSmall（无 6dp token 归 4dp）；死代码 SegmentedTextToggle 不动；清 3 文件失活 import。视觉零变化（除日志高亮 6→4dp）。
- **CI 34580067226 success，用户已自打包**（新包指向 96e76d2）。watch 命令曾被 shell 抽风误断一次（run 实为 in_progress），直查 `gh run view` 确认后重挂解决；`date` 命令本环境缺失，时间取自系统提醒。
- **🎉 今日 MD3 全链终版（8 笔全推送 CI 绿）**：77e955b（滑条/分段/复选框）→ 5a9d7ff（父级分段）→ 9e51e16（8 处漏网弹窗）→ 435afc9（confirmButton 修复）→ 7634e23（LoadingDialog）→ f92b460（字号 11 处）→ e0baff6（静态 Bold 12 处）→ 96e76d2（圆角 6 处）。组件级+形式级全部官方/token 化。
- **遗留灰地带（用户整体看问题时再定）**：elevation 硬编码 2 处、SettingsSearch 透明边框填充式搜索框、终值行 2dp 间距、网格外间距 ~30 处（用户裁定保留）。
- 09-11 旧定案条目（胶囊轨道 10dp/软槽浮块/32dp 触摸区/labelFontSize 等）**全部作废**，以本日终版为准；规范文档《MD3 UI 规范约定.md》为唯一现行标准。

## 17:15 "还是1400"排查 + 重新打包（34583313526）+ 用户换窗口
- **"1400"真相**：用户 16:41 自打的 tag 1.26.09111641 与手动 dispatch 的 Build Release run 34580407595 其实**全部正确**（mumu_tts_github@96e76d2，16:47 发布 Latest Release 1.26.09111641.apk）——"1400"= 1.26.09111400（14:00 构建的旧包），是 **Release 列表点错下载**（两个版本名只差中间几位）。
- **打包机制实锤**：versionName 是**构建时刻时间戳**（app/build.gradle:20 `1.26.${MMddHHmm}`），与 tag 无关；versionCode 硬编码 1510；Build Release 支持 tag push（1.26.*）和 workflow_dispatch 两条路；release job 用 ver_name 建同名 Release。**APK 文件名≠代码版本，看 release 的 targetCommitish 才是代码版本**。
- 按用户要求重打：gh workflow run release.yml --ref mumu_tts_github -f build_dev=false → run 34583313526（后台盯），产物应为 1.26.091117xx 新 release，版本号独特防再错。
- 用户要换窗口，已给新窗口交接提示（见下）。

## 17:35 重打包 run 34583313526 收官（新窗口确认）
- run 34583313526 **success**（build 6m47s + release 19s）；新 Release **1.26.09111716**（Latest），targetCommitish=**96e76d2**（终版 commit 核对无误），资产 `TTS-Server-v1.26.09111716.apk`（18.98MB，sha256 0d8928d1…）。
- 下载认准版本号 **1.26.09111716**（别再碰 1.26.09111400 旧包）。工作区干净、head=96e76d2、无待推内容。等用户装包反馈 MD3 视觉问题。

## 18:05 用户清理了 Actions 历史与 11641 Release + 对比基线盘点
- Actions 历史被用户手动清理（全仓库只剩 34580067226/34583313526 两个 run）；Release 1.26.09111641 也已删。
- 现存对比包：11153=05894d8（自绘壳终版）、11234=6431ed7（壳换官方一步）、11252=082af29（壳彻底官方）、11400=**5a9d7ff（内容组件 MD3 起点）**、11716=96e76d2（全量终版）。

## 18:38 装机反馈第一轮（da679cd，未推送，等「推」）
- **卡片（list/Item.kt）**：右上角标签两版核对**一模一样**（bodyMedium 14sp+Medium+描边胶囊，一直没动），用户自我修正不改；标题唯一实质变化=e0baff6 撤 Bold。
- **用户终裁：titleMedium 字重回 700（原版观感）**——走主题层 Type.kt 一处改全局（非硬编码），已写特例许可注释；字号/行高/字距保持官方 16/24/0.15。影响全 app 所有 titleMedium（弹窗标题 headlineSmall 不受影响）。
- **重叠 bug 修复**：长标题原穿到标签底下（nameRef 无 end 约束+Clip 硬裁）→ end.linkTo(targetRef.start)+Ellipsis+width=fillToConstraints；targetRef 改常驻 Box（空标签 0 宽，约束不失效），TagScreen 挪进 Box。
- **LoadingDialog 居中**：Column 加 CenterHorizontally、Text 加 textAlign=Center+fillMaxWidth（用户："提示应该在中间，尤其是圆圈"）。
- 待办：等用户说「推」→ CI；卡片字重 700 装机再验，不合适主题层一处改回。

## 18:46 字重回退扩为全量（0f09ffb，未推送，等「推」）
- 用户确认方向："改 MD3 后看不惯就回原版观感，当时也挺和谐" → 拍板**一次到位全回**。
- da679cd 只回了 titleMedium 档；本笔 `git revert --no-commit e0baff6` 补回剩余 10 处硬编码 Bold（SectionCard titleSmall/备用+当前角标/插件角标/删除+清除确认按钮/error 正文×2/GitHub 链接/正则键盘按钮）。Item.kt 冲突手动解（保留 Ellipsis+右边界修复，标题不加 fontWeight 靠主题层）；ConfigDeleteDialog 正文 titleMedium 冗余 Bold 撤（主题层已 700）。
- **"回原版"的边界**：只回字重（e0baff6 整笔撤销效果）；字号 token 化（f92b460）、圆角 token（96e76d2）等其余收官链**不动**。
- 待推：da679cd + 0f09ffb 两笔，等用户说「推」→ CI → 装包看全貌。

## 18:54 已推送 + CI 绿
- push 96e76d2..0f09ffb（da679cd+0f09ffb），CI 34591157691 **success**。未打 tag，等用户装包。
- 用户在看的"分组字号怪"定位：f92b460 把 GroupItem 18sp→titleMedium 16（降2sp）、SubGroupHeader 17/16/16→16/14/16。**建议先装 700 字重包再看**，还怪就把这两处字号回原版手调值（特例许可）。

## 19:03 字号回原版第一波（5122524，未推送，等「推」）
- 用户指认最明显处：**一级分组 GroupItem 回 18sp**、**替换规则列表项 replace/Item 回 15sp**（特例许可注释，sp import 补回）。
- 未动（等装包反馈）：SubGroupHeader 17/16/16→16/14/16、插件管理 15→14、设置项 15→14、卡片描述 13→12 等 f92b460 其余处。

## 19:12 字号回原版第二波（5e94afc，未推送，等「推」）
- 用户澄清指认：插件管理没问题（15→14 不回）；**替换规则全部回原版**（实际只有 replace/Item 一处动过，已在 5122524 回完）；**子分组头回 17/16/16 三档**、**卡片描述行回 13sp**（HtmlText fontSize 与 style 双处，主次差恢复 13 vs 12）。
- 设置/朗读规则页字号 token 化维持不动。sp import 各补回。
- 试听弹窗分类 Chip（12→14sp 唯一升大的）：用户拍板**不回**，维持官方 labelLarge 14sp。至此字号回退波次结束，待推=5122524+5e94afc。

## 19:30 日志弹窗反馈（2459691，未推送，等「推」）
- 用户指认三处：①父级胶囊"更…/音…"截断 ②滑杆手柄竖条比粗轨道高出太多 ③滑杆标签字小。
- **①官方 SegmentedButton 硬限制实锤**：内部 label 用 weight 布局，只支持定宽/均分，"宽度随文字收缩"必截断。用户拍板**均分撑满**，equalWidth 参数**删除**（防再踩，注释已写勿加回）；LogQuickPanel 撤传参。
- **②手柄 44dp→24dp**（用户澄清是高度不是长度；官方 M3 竖条 thumb=4×44、轨道 16dp）。LabelSlider 两处重载 Slider 传自绘 thumb（仅尺寸：4×24dp 圆角2dp，颜色 primary/onSurface(38%)；轨道仍官方粗轨道）。thumbHeight 默认参数一处改全 app。
- **③滑杆标签+数值 bodySmall 12→bodyMedium 14sp**（全 app 统一）。
- 待推累计：**5122524 + 5e94afc + 2459691 三笔**。

## 17:41 CI Action 运行历史清理（用户要求：release/test 各只留最新一条）
- **gh run delete 逐条删**（该命令一次只接受 1 个 ID，用 for 循环）。共删 37 条：release 10 条（留 34583313526）、test 27 条（留 34580067226）。
- 核验：两 workflow 现各仅剩 1 条最新 success 记录。删运行历史**不影响已发布 Release/APK**（资产独立）。
- 注：本次清理前已确认重打包新 Release `1.26.09111716` 为 Latest，且用户之前"误下 1400 旧包"的根因是 Release 列表点错，非构建问题。

## 19:36 父子分段同款定案
- 用户问"父胶囊和子胶囊一模一样行吗"（父级「更换发音人/音频参数」与子级维度切换现同为官方 SegmentedButton）→ 拍板**保持一模一样**，层级靠位置区分，不做 FilterChip 等形态区分。**勿再提父子分段换形态/区分样式的提案**。
- 本地待推仍为 3 笔：5122524（分组18sp+替换规则15sp）+ 5e94afc（子分组头17/16/16+卡片描述13sp）+ 2459691（父级分段均分撑满+滑杆手柄24dp+滑杆标签14sp），等用户说「推」。

## 20:07 更换发音人字号排查（重要机制发现）
- 用户指认搜索框提示字比分类框值大 → 像素实测证实（56px vs 50px，即 16sp vs 14sp）。
- **根因（机制，全局适用）**：官方 AlertDialog 的 text 槽会把 LocalTextStyle 设为 bodyMedium 14sp → 弹窗内所有 TextField 的**值文字**默认 14sp；而 OutlinedTextField 的 **label 空置中态官方写死 bodyLarge 16sp**（不接正文槽），聚焦后缩 12sp。所以弹窗里"提示字比内容大一圈"是官方原生组合，代码零硬编码。
- 修复（4b0eb7a）：LogQuickPanel 搜索框 label→**placeholder**（吃正文槽 14sp，与分类框/候选行对齐；输入后提示消失，可接受）。**勿回退成 label**。
- 排查手段：PIL 自适应阈值量字形像素高度（阈值=背景/字芯中值），38% 透明灰字必须自适应阈值否则量小。
- 本地待推增至 4 笔：5122524+5e94afc+2459691+4b0eb7a。
- 补充（61dab68）：AppSelectionDialog 列表条目显式 bodyLarge 16sp→bodyMedium 14sp，与弹窗内字段值（正文槽 14sp）对齐；一处改全 app 所有选择弹窗（插件/分组/分类）。用户指认的"分类框 14、点开列表 16"错位就此收口。
- **全 app 静态扫查（30 个 AppDialog 文件）**：弹窗正文槽字号错位唯一残留 = TagSwitchDialog 两层选择列表（24d3433 已对齐 14sp）。其余显式大字号均为有意设计（AboutDialog 应用名、AppUpdate/BackupDialog titleSmall 小节标题、发音人名 titleMedium 强调、非弹窗环境各处）。结论：**"AlertDialog 正文槽 14sp 环境 vs 显式样式"错位类问题已全量清零**，用户无需逐页肉眼扫。
- 本地待推 6 笔：5122524+5e94afc+2459691+4b0eb7a+61dab68+24d3433。
- 20:21 用户说「推」→ push 时发现远端已在 24d3433（6 笔已在远端，疑用户在别处已推/或首次 push 实际生效被误报 rejected），ls-remote 直查核实。CI 编译验证 run 34598567112（head=24d3433）in_progress，后台盯守中（task OEMIW0）。绿后代打打包或等用户 tag。
- 20:25 CI run 34598567112 **success**（head=24d3433，6 笔全绿）。等用户定打 tag/打包方式；产物版本号将是 1.26.0911 20xx 开头，认准新号勿下 11716 旧包。
## 20:37 分类下拉 0 项隐藏（3e4a4b5）
- 用户发现分类下拉有四个分类不标数量 → 查明是 09-09 定案「0 项不标数量」；用户改判：**0 项分类直接隐藏**（不标会被误读成信息缺失）。已实施：categoryOptions 移入绑定模式内按 poolEnabled 动态生成，全部恒首位、有货分类带（N项）；选中分类恰好 0 项时 AppSpinner 自动回落「全部」（沿用既有九类外→全部语义，无幽灵值）。**勿再回「0 项不标/标（0项）」方案**。
- 用户已自打 tag 出包（含前 6 笔）；本笔未推，需再推+再打一次包才能看到。

## 20:40 发音人搜索范围修正（8490442）
- 用户：搜索应能搜"女青年01晓晓"（标签名+名字）。根因：绑定模式候选过滤只匹配**标签名** `it.contains(tagSearch)`，而候选行展示的是 `tag + cfgName`（标签名+配置项 displayName），搜"晓晓"必然搜不到。
- 修复：过滤条件扩为 `tag.contains(q) || enabledConfigEntityByTag(tag)?.displayName?.contains(q)`（标签名或配置项名任一命中）；**搜索态不再补顶当前绑定项**（否则顶部挂不匹配项破坏搜索语义，非搜索态照旧补顶防丢值）；提示文案改「搜索标签名/名字（当前范围内）」。
- 全文件仅此一处搜索框（旁白模式无下拉无搜索，不涉及）。
- 本地待推 2 笔：3e4a4b5（分类下拉0项隐藏）+ 8490442（本笔），均需再推+再打包才能看到。
- 20:45 候选列表高度（58c2f31）：绑定模式 220dp / 旁白模式 300dp 两个手调固定值 → 合并为弹窗顶部共用的 `maxListHeight = 屏高 × 40%`（800dp 屏约 320dp ≈13 行），配合外层 85% 屏高上限自适应，勿再写死值。用户同时重报"搜索结果里当前发音人始终钉第一行"→ 确认即 8490442 已修的那处（旧包未含），全 app 补顶逻辑仅 LogQuickPanel:517 一处。
- 本地待推 3 笔：3e4a4b5 + 8490442 + 58c2f31。
- 20:48 用户已自打包装机：**1.26.09112033 = 24d3433**（含 96e76d2→24d3433 共 8 笔：da679cd/0f09ffb/5122524/5e94afc/2459691/4b0eb7a/61dab68/24d3433）。**不含**未推的 3e4a4b5/8490442/58c2f31（分类0项隐藏、搜索匹配名字、列表高度自适应）。

## 21:20 回退到 1400 观感（9b80506）
- 用户看了新包后要求"退回今天的历史版本"。定性今天的谱系：①表面 MD3=082af29（包11252）②彻底化=5a9d7ff→96e76d2（包11400/11716）③今晚"查缺补漏大力改"=da679cd→24d3433（包12033）。
- 用户选 **1400（`5a9d7ff`）** 为目标，并拍板：带上 9 处弹窗官方化（9e51e16）、保留 5 项真 bug 修复、保留 4 项曾抱怨过的微调。
- **关键发现（省了大量工作）**：逐文件 diff 后确认 **1400 与现状主体观感本就一致**——内容组件在 1400 已是官方形态，今晚多笔本就是往回捞（字重/18sp/17-16-16/13sp/15sp）。实际只剩 2 项差异：圆角 6 处、主题层 titleMedium 字重。
- 已撤（9b80506，7 文件 12+/18-）：①圆角 6 处 token→手调值（GroupItem 4dp、LogScreen 6dp、ListManager 2×8dp、LogQuickPanel 2×8dp；其中仅 LogScreen 6dp 有 2dp 视觉差）；②Type.kt 撤 titleMedium=700，Item 卡片标题与 ConfigDeleteDialog 正文改回自身 `fontWeight=Bold`。补回 3 文件 RoundedCornerShape import。
- **未撤（用户认为没问题/明确不改）**：f92b460 残留的 5 处字号（设置页/插件管理/参数区/AuditionDialog Chip=用户明确"不用改"）。
- 本地待推 4 笔：3e4a4b5 + 8490442 + 58c2f31 + 9b80506。推送后需重打包（认准新版本号）。

## 21:32 字号回原版收尾（3ef36f7）
- 用户回复"一并回原版"（指我上条列的 f92b460 残留 5 处）→ 实际回 3 文件 4 处：**SettingsWidgets 设置页副标题 14→15sp**、**PluginManagerScreen 插件名与提示 14→15sp**、**AuditionDialog 试听分类 Chip 14→12sp**（推翻当日"Chip 不用改"裁定）。
- 澄清两点：①"参数区"= AudioParamsDimensionSection 在 f92b460 只清了一个失活 sp import（实际代码无字号），无需回；②SpeechRuleEditScreen 两处 labelLarge.copy(14.sp)→labelLarge 是**零视觉差**（labelLarge 官方即 14sp），保留不写回冗余写法。
- LabelSlider 标签 14sp 维持（2459691 用户拍板"滑杆前方文字大点"，属保留微调，不回 12sp）。
- 结果：f92b460 撤的 11 处硬编码 sp **视觉上已 100% 回原版**；全 app 手调 sp 清单 = GroupItem 18 / SubGroupHeader 17-16-16 / list Item 13 / replace Item 15 / SettingsWidgets 15 / PluginManager 15×2 / AuditionDialog Chip 12。
- 本地待推 5 笔：3e4a4b5 + 8490442 + 58c2f31 + 9b80506 + 3ef36f7。

## 21:39 列表类弹窗宽度回宽版（d56d2e4）
- 用户："改回md3弹窗宽度会变窄这个能回从前吗？我不想变窄" → 拍板**只回那 7 处、只回宽度（圆角保持官方 28dp）**。
- **事实澄清（重要）**：全 app 弹窗宽度**只有 7 处**因 9e51e16 变窄——BatchTagDialog 0.92 / GroupEditContentDialog 0.9 / GroupTreePickerDialog 0.9 / ListManagerScreen 四处 0.92（移动启用配置、多选移动子分组、单移动子分组、转为一级分组）。它们原自带 `usePlatformDefaultWidth=false`+0.9x 被 9e51e16 当自定义撤掉。**其余 29 处走 AppDialog 垫片的弹窗宽度从未变过**（旧自绘壳同样是 `DialogProperties()` 默认），PluginManagerScreen 两处撤的是 containerColor 白底不是宽度。ForwarderWebDialog 的 false 是转发器弹窗，保留不动。
- 已恢复（d56d2e4，4 文件）：7 处加回 properties=false + fillMaxWidth(原值 0.9/0.92)；补回 DialogProperties import。圆角维持官方。
- 本地待推 6 笔：3e4a4b5 + 8490442 + 58c2f31 + 9b80506 + 3ef36f7 + d56d2e4。

## 21:47 推送 + tag 触发打包收官（用户「那就推送，打包」）
- `git push origin mumu_tts_github`：`24d3433..d56d2e4`（6 笔全上远端）。编译验证 run `34606318757` **success**（build 2m22s，仅 Node.js 20 deprecation 提醒）。
- **打 tag 触发 Build Release**（用户明示打包 = 授权代打）：tag **`1.26.09112150`**（规格 1.26.月日时分）→ push tag → release.yml 的 `on.push.tags: '1.26.*'` 命中，Build Release run `34606626947`（仅 build app，tag 路径 build_dev 不生效）→ build(app) 6m54s ✓ + release 15s ✓。
- **发布产物版本名 `1.26.09112151`**（Latest，2026-09-11T13:57Z）。注意：tag 与 Release 版本名可能差 1 分钟——versionName 是**构建时刻**时间戳（app/build.gradle `1.26.${MMddHHmm}`），与 tag 无关，别被差一位吓到。
- **用户偏好（新，记住）**：用户明确"通过提交触发打包我喜欢，能看到版本号"——**以后默认走 tag 推送（1.26.*）触发打包**，优于 workflow_dispatch（手动触发那次版本号不直观/还默认带 dev 共存版）。
- 用户已装机该包（1.26.09112151 = d56d2e4，含 6 笔：分类0项隐藏/搜索匹配名字/列表高度自适应/回退1400观感/字号回原版/弹窗宽度回宽）。
