# 2026-09-09 工作日志

## 前情接入（WorkBuddy 接手，前序为 zcode/Trae 会话）
- 读了 `.zcode` 记忆索引（20 条）+ `E:\TTS\技术文档\` 16 篇 + 根目录两份 trae 交接文档
- 09-09 前序批次已推绿：最新 `355d206`（TtsPluginEngineV2 eval 判空保留原值 `?: plugin.xxx`，修 pluginId 被空串覆盖）
- 未决悬案：**旁白失败没跳 duihua 备用**——根因未实锤，需复现抓日志（找"使用备用发音人：旁白 → duihua"），e348821 盲改已 revert，禁止再盲改

## 本次操作
- 用户把 `tts-mumu/技术文档/`（16 篇 md）移出仓库到 `E:\TTS\技术文档\`，另有若干文件删除
- commit `733b7ad` 提交文档迁出（3235 行删除），推送 355d206→733b7ad 成功
- 推送前 `git ls-remote` 直问远端核对 HEAD（355d206），本地领先一提交，快进推送
- **纯文档改动不触发 CI**：`test.yml` paths 白名单只含 lib-*/app/src/build-logic/gradle，不含 md —— 无需等编译
- 顺带发现用户已在 03:11 自行触发 Build Release 且 success（`34306173741`）

## 记忆更新
- `MEMORY.md` 新增「文档归口」章节：技术文档路径变更、旧路径失效、test.yml 不触发文档 CI
- 清理「待拍板」两项（已于 09-07~09-08 结案）

## 间歇性"无声卡死"排查与观测加固（13:00~14:15）
- **现象**：阅读点朗读，日志停在"获取成功"后一行不再出、不出声；有时三段日志重复三遍；去阅读界面点开即恢复。间歇、自愈、与设备切换无关
- **排查结论**（静态全链路）：三段全部合成成功，卡点收敛为"消费端 audioAvailable 不可取消阻塞，系统侧不消费"（音频焦点被占/进程冻结省电/路由异常），全链路无超时无日志；头号环境嫌疑=进程冻结（KeepAliveService 注释即为此而造，前台服务通知受 isForegroundServiceEnabled 控制）
- **commit `db9d528`**（已提交未推）：①audioAvailable 打点+合成协程看门狗（阻塞≥10s 每10s WARN）②SysttsFilter 白名单放开 TtsManager+DefaultResultProcessor（合成心跳日志首次可见）③350s 总保护超时补 logW+callback.error（原先静默 done）
- **待办**：等用户说「推」→ CI → 打包装机 → 复现时日志栏直接显示卡点；合成器 logger 名=TtsManager（MixSynthesizer.global 传入 context）
- 关键事实：requestTimeout 默认 60s（SynthesizerConfig 全局兜底 300s）、streamTimeout=max(3×,30s)、maxRetry=3、segmentPauseMs 默认 0、非流式才有"获取成功"日志
- **新线索（14:22 用户反馈）**：只有"标题批"卡（衔接标题音效+短标题+衔接正文音效），其他段落点朗读没事 → 标题批独有特征=2 段本地音效+短文本，合成约 1 秒内全部完成进入纯播放期（与消费端 audioAvailable 卡死假说兼容）；验证路径=①用户临时关衔接标题音效规则二分实验 ②推 db9d528 装机后看门狗日志直接定位

## 19:36 日志配色与噪音治理（参谋，未动代码）
- 用户诉求：①朗读规则/插件日志想换色（现全挤 INFO 绿）②插件 E/W 不勾选也要主列表可见③"干问音频块"刷屏能否日志端限制④图3 onLoadData/onLoadUI D 级刷屏
- 代码事实：颜色在 `lib-common/LogLevel.kt toArgb()`（按级别）；LogEntry 已有 `isPluginLog`/`isSpeechRuleLog` 字段，LogScreen.kt 可按来源染色；过滤在 `TtsLogViewModel.filteredLogs`（showPluginLogs 默认隐藏会连 W/E 一起藏）；噪音治理建议做在 display 层折叠（连续同前缀合并 ×N），插件 JS 日志不必改插件
- 给出方案：规则→紫 #7E57C2/#B39DDB，插件→青 #00897B/#4DB6AC，级别语义色（红黄蓝）优先于来源色；W/E 始终显示（改 filteredLogs 豁免）；DEBUG 默认可考虑收进筛选
- show_widget/read_me 连接失败（connector-proxy fetch failed），对比图未出，方案以文字交付

## 19:59 日志配色与噪音治理（定稿实施，commit 25cbf5f，未推送）
- 用户否决亮青/亮紫（抢主流程绿），要低调：比绿安静、比石板灰略突出、避开棕褐 #7D6B5D；选定**方案二**：插件=灰青 #4E6E68/深#8FA9A3（DEBUG 淡灰青 #829895/#75908A），规则=灰紫 #6E5E78/深#A795B1（DEBUG 淡灰紫 #9487A0/#90819F）
- 实施 ①LogScreen 来源优先着色（ERROR/WARN/SUCCESS 级别语义不动）②filteredLogs 不勾选时旁路放行插件/规则缓冲的 ERROR+WARN（默认视图错误可见）③routeEntry 连续同模式折叠：数字归一化(\d+→#)与末条相同则并入 repeatCount+1，LogEntry 加 repeatCount 字段，渲染行尾「×N」，message 保留串内最后一条；折叠仅作用插件/规则缓冲，主列表不折叠
- 改动文件：lib-common LogEntry.kt / app TtsLogViewModel.kt / LogScreen.kt；技术文档「日志栏架构.md」过滤逻辑+着色表已同步更新
- 待办：等用户说「推」→ push + CI 验证（本机不编译）；上轮 show_widget 失败本轮成功，配色对比图已出

## 20:10 快捷面板音频参数区超高修复（commit eec5e40，未推送）
- 根因：三层滑杆平铺约600dp+面板写死heightIn(600dp)，超出屏幕可用高度，底部重置/应用被裁（用户截图）
- 改法：音频参数区加第二级分段（配置项/插件/全局，一次一层，无插件源两项+索引映射1→2）；面板上限改屏幕85%；层间分隔线撤除
- 文件：LogQuickPanel.kt；技术文档已同步；现共两个未推送 commit：25cbf5f（日志配色/旁路/折叠）+ eec5e40

## 20:29 快捷面板补三层现值总览行（commit 44ec936，未推送）
- 用户反馈：分段后看不到全部层数值 → 按其提案在分段下方加一行，格式=配置项卡片参数行（PluginDescriptor.desc 09-07 定稿：配置→插件→全局、≠1.0 才显示、×连接、small 层标、全默认「无设置」），值跟随草稿实时更新
- 现共三个未推送 commit：25cbf5f / eec5e40 / 44ec936；已把「UI 改动先提案后动手」写入 MEMORY.md 硬约束第 9 条

## 21:30 音频参数按维度改版+参数跟随（commit f26a2b3，未推送）
- 用户拍板：②按维度一组重置/应用（该维三层一起落库）③插件接管维度(pluginHandlesX)自动隐藏插件/全局滑杆换说明行 ④删三层现值总览行(44ec936被翻) ⑤AudioParamsDialog 顶部加当前发音人+▶试听（草稿试听，应用才落库）；音高进插件/全局层（翻掉09-07旧决定）
- 新建共用组件 AudioParamsDimensionSection.kt（widgets 包），AudioParamsDialog（卡片菜单+编辑页两入口同改）与 LogQuickPanel 共用
- LogQuickPanel 参数跟随 paramsTarget：绑定=绑定(含暂存)标签启用配置项、旁白=暂存候选配置项、无暂存=本配置项；目标切换整体重载三层草稿+插件元数据；旁白确认=voice+参数草稿一并写入本配置项；顶部发音人名/终值/试听同步跟随
- 技术文档日志栏架构.md 快捷面板段已同步（按维度二稿+参数跟随）
- ⚠️环境坑：同一消息里并行多个 Edit 只有最后一个真正落盘，其余报成功但文件未变（本轮实锤，靠复读发现）；后续 Edit 必须串行逐个发

## 21:57 卡片参数行只留最终值+层名改「发音人」（commit d12bc1d，未推送）
- 用户判定：括号后缀是格式问题不是命名问题；推翻 09-07 卡片链式格式（带 `<small>(配置)</small>`×连接），退回之前格式：三维恒显、数字 `<b>` 加粗与标签同字号、管道 `|` 分隔、1 位小数（参照用户截图）
- 09-07 同步翻掉的两个东西：①「无设置」占位行（无意义，三维都 1.0 时仍显示）②音高 `globalVal=1f` quirk（卡片音高列缺全局层未参与乘积——本次顺手补齐）
- 层名改「发音人」：audio_params_tag_config「配置」→「发音人」→AudioParamsDimensionSection 的 LayerSlider 标签生效；audio_params_card_hint「配置项」→「发音人」；audio_params_apply_config_toast「本配置项」→「该发音人」（中英同步）
- 影响面：LogQuickPanel 顶部「最终」行口径不动（非 1.0 才显示）；弹窗内按维度滑杆天然提供层分布（卡片看不到但弹窗里清楚）
- toScale 导入可清理（留作警告无害）
- 现共 5 个未推送 commit：25cbf5f / eec5e40 / 44ec936 / f26a2b3 / d12bc1d

## 22:07 终值行三维恒显（commit 9e80d00，未推送）
- 用户：「都改为全显吧，语速、音量、音高都列出来，都是最终值」→ 弹窗 AudioParamsDialog 与 LogQuickPanel 顶部终值行均改为三维恒显
- 新增顶层函数 `buildFinalParamsText(prefix, speed, volume, pitch): AnnotatedString`（放 AudioParamsDimensionSection.kt），用 buildAnnotatedString + SpanStyle(FontWeight.Bold) 实现数字加粗；两处调用点共用，删掉各自的 finalDims 过滤块与 audio_params_none 占位分支
- 全口径统一：卡片参数行 / 弹窗终值行 / 面板终值行 = 三维恒显 + 1 位小数 + 管道 | 分隔 + 数字加粗 + 最终值（配置×插件×全局，尊重 pluginHandles 路由）
- audio_params_none 字符串两处调用已撤（仍被别处使用故保留）；技术文档日志栏架构.md 已同步
- 现共 6 个未推送 commit：25cbf5f / eec5e40 / 44ec936 / f26a2b3 / d12bc1d / 9e80d00

## 22:39 终值格式差异化+删除 x 乘号（commit ef76aea，未推送）
- 用户拍板：卡片不加前缀保持 d12bc1d 样式；弹窗/面板/编辑页三处终值行改回旧「最终：」逗号+2位格式；删除所有 x 后缀（包括 AuditionDialog 试听弹窗、SystemTtsService 日志发音人参数）
- 卡片：`PluginDescriptor.desc` 撤回前缀，HTML 字符串直接 = 语速:<b>%.1f</b> | ...（无前缀+管道+1位+加粗）
- 弹窗/面板/编辑页：`audio_params_final` 复活 = `最终：语速%1$.2f，音量%2$.2f，音高%3$.2f`（英文 Final: speed ...），三处统一用 `stringResource(R.string.audio_params_final).format(...)`
- AuditionDialog + SystemTtsService：删 `%.2fx` → `%.2f`，其他逻辑（≠1.0 过滤/连接符）不变
- 删 9e80d00 新建的 `buildFinalParamsText` 函数本体 + 5 项不再用的 import（AnnotatedString/SpanStyle/FontWeight/withStyle/buildAnnotatedString）+ 两个调用方的 import
- 现共 7 个未推送 commit：25cbf5f / eec5e40 / 44ec936 / f26a2b3 / d12bc1d / 9e80d00 / ef76aea

## 23:05 六处参数显示完全统一（commit ab98cbc，未推送）
- 用户拍板 B 案（完全统一）+ 值按实际精度显示 + 试听弹窗项间改逗号
- 新增 `lib-common/common/utils/DecimalUtils.kt` 顶层扩展 `Float.toParamText()`：先 %.2f 再 trimEnd('0')，末尾是小数点则补 0 → 1.00→"1.0"、1.50→"1.5"、0.97→"0.97"、1.04→"1.04"
- 六处统一为「最终：语速1.0，音量1.85，音高1.2」：卡片 PluginDescriptor.desc（撤管道+加粗+1位）、弹窗 AudioParamsDialog、面板 LogQuickPanel、编辑页内嵌 PluginTtsUI、试听弹窗 AuditionDialog（逗号替代空格）、日志发音人信息 SystemTtsService
- `audio_params_final` 占位符 %.2f → %1$s（中英同步），由调用方传 toParamText() 结果
- 技术文档「日志栏架构.md」终值行段已同步
- 现共 8 个未推送 commit：25cbf5f / eec5e40 / 44ec936 / f26a2b3 / d12bc1d / 9e80d00 / ef76aea / ab98cbc

## 23:20 删编辑页内嵌旧滑杆区+英文同步+清死串（commit 66faed5，未推送）
- 用户拍板：②英文版同步 ③删死字符串 ④删掉旧滑杆区（=RemoteAudioParamsSection）
- **删文件**：`RemoteAudioParamsSection.kt` 整体删除（编辑页内嵌插件层/全局层滑杆，与顶部按维度弹窗功能重复）。注意它里面有 internal 顶层 `snap()` 被 AudioParamsDialog 依赖 → 先迁到 `AudioParamsDimensionSection.kt` 再删文件，否则断链（PluginTtsUI 内另有同名局部 snap，与顶层不冲突）
- **保留** ThreeLayerFinalLine 终值行（唯一"所见即所播"），去掉 appliedTick 参数改随 entity 刷新
- **英文同步**：补 `audio_params_apply_dim_toast`（此前英文版完全没有，调即崩）；tag_plugin "P"→"Plugin"、tag_global "G"→"Global"（单字母是躲卡片括号后缀长度的旧产物，格式已废弃）
- **清死串**：中英各删 19 条（删代码前 12 条已死，删内嵌区后又增 7 条）；清理后中英各剩 8 条一一对应，代码 8 处引用全部有定义
- 现共 9 个未推送 commit：25cbf5f / eec5e40 / 44ec936 / f26a2b3 / d12bc1d / 9e80d00 / ef76aea / ab98cbc / 66faed5
- **更正（23:47 用户截图证实）**：66faed5 删的内嵌滑杆区实为**死代码**——09-07 用户已定稿插件 TTS 编辑页不出滑杆区（只留顶部「音频参数」按钮弹窗），QuickEditBottomSheet 里 `if (!isPluginTts)` 才渲染 ParamsEditScreen，插件 TTS 永远不渲染。删除=零 UI 变化，勿再向用户描述为"删了可见区块"。插件 TTS 调参唯一入口=顶部按钮弹窗
- 23:54 追加 commit 0278b13：用户拍板"都删掉只留顶部弹窗"→ IConfigUI.ParamsEditScreen abstract→open 空默认（本地 TTS/BGM 仍各自 override 渲染 PCM 等专属设置），删 PluginTtsUI 的 ParamsEditScreen override + ThreeLayerFinalLine + 5 个死 import；净 -119 行
- 00:17 追加 commit dc5dd22：**废除插件接管判定**（用户拍板"人工编码删除+都显示可调"）——库标记 pluginHandlesX 恒 false（全仓无写 true 路径，纯预留未启用）；删 parameterRoute 千问硬编码条目（⚠️千问音量处理方从插件内→宿主后处理，装机需实测千问调音量）、UI handlesX 链全删（维度弹窗 TakenOverHint/三处 handles 传参/终值恒乘积）；库字段保留；三点菜单插件音频参数弹窗本就三杆全显未动。现共 **11 个未推送 commit**：25cbf5f / eec5e40 / 44ec936 / f26a2b3 / d12bc1d / 9e80d00 / ef76aea / ab98cbc / 66faed5 / 0278b13 / dc5dd22
