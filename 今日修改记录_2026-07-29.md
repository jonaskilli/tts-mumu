# 今日修改记录（2026-07-29）

分支：`mumu_tts_github` → `origin/mumu_tts_github`
提交数：16（含 7-27 起合并入主干的全部改动）
版本号：`1.26.072910`（7月29日10点）
CI：GitHub Actions 自动打包发布已全链路打通

---

## 一、性能优化（消除选中/拖拽卡顿）

### 1. 选中选项消除卡顿，多选用 derivedStateOf 减少重组
- 选中分组 / 朗读规则 / 插件 / 替换规则等选项时不再全列表重组。
- 每个列表项用 `remember(item.id) { derivedStateOf { selectionMode && item.id in selectedIds } }` 单独计算选中态。
- 涉及：`SpeechRuleManagerScreen`、`ListManagerScreen`、插件管理、替换规则管理等。

### 2. 各界面拖拽排序改 IO 线程批量更新
- 拖拽排序后的 DB 更新统一走 `scope.launch(Dispatchers.IO)` 批量执行，避免主线程阻塞。
- 涉及：`ListManagerViewModel`、`ListManagerScreen`、朗读规则 / 插件 / 替换规则拖拽。

### 3. 导入改 IO 线程异步执行
- 导入发音人 / 朗读规则 / 插件 / 替换规则全部改 IO 线程异步执行，导入大量数据不再卡 UI。

### 4. ManagerActivity 与 MainActivity 的 onSave 回调改 IO 线程
- 保存配置不再阻塞主线程。

---

## 二、新功能

### 5. 同标签发音人去重
- 选中同 `tag` 的另一个发音人时，自动禁用同 tag 旧项，**只保留后选中的**。
- 涉及：`ListManagerScreen`、`ListManagerViewModel`。

### 6. TTS 界面与角色管理优化（`e2882fd` / `4de1c28`）
- 工具箱 `ToolBoxScreen` 体验优化。
- `ControlBottomBarVisibility` 简化（-49/+60 行）。
- `PagerDestination` 页索引调整。
- `MainPager` 开启左右滑动切换。
- 涉及：`MainPager`、`PagerDestination`、`ToolBoxScreen`、`ControlBottomBarVisibility`、`strings.xml`（中英文）。

---

## 三、Bug 修复（主线程阻塞 / 资源 / 协程泄漏）

### 7. 主线程数据库操作修复
- 分组删除 / 重命名 / 展开状态切换 / 启用切换等 DB 操作全部从主线程移到 IO 线程。
- 涉及：`ListManagerScreen`、`ListManagerViewModel`。

### 8. OkHttp Response 与 InputStream 资源泄漏
- 网络响应统一用 `Response.use { }` 包裹，确保关闭。
- 涉及：`ConfigImportBottomSheet` 等。

### 9. LaunchedEffect 内嵌套 scope.launch 的协程泄漏
- 移除 `LaunchedEffect` 内嵌套的 `scope.launch`，直接在 `LaunchedEffect` 块内执行 suspend 函数。
- 涉及：`LogScreen`、`RestoreDialog`。

### 10. ReplaceRule / ReplaceSortDialog 主线程阻塞
- 排序、保存等操作改 IO 线程。

---

## 四、CI / CD 全链路打通（GitHub Actions 自动打包发布）

### 11. release.yml 自动打包触发（`292536c`）
- `mumu_tts_github` 分支 push 且修改了 `CHANGELOG.md` 时自动触发 `Build Release` workflow。
- 支持 `workflow_dispatch` 手动触发。

### 12. 修复 gradle-wrapper.jar 缺失（`7ae6cb2`）
- 添加缺失的 `gradle/wrapper/gradle-wrapper.jar`，解决 CI 报 `Could not find or load main class org.gradle.wrapper.GradleWrapperMain`。
- 修改 `.gitignore` 添加 `!gradle/wrapper/gradle-wrapper.jar` 例外，确保纳入版本控制。

### 13. 修复 settings.gradle pluginManagement 缺 google()（`a92cc37` / `6324e67`）
- 在 `pluginManagement.repositories` 中添加 `google()` 与 `gradlePluginPortal()`，解决 `Plugin [id: 'com.android.library', version: '8.8.1'] was not found`。

### 14. 修复 KSP 2.1.10-1.0.29 无法解析（`246225a` / `14d1ce4`）
- KSP 插件 `com.google.devtools.ksp:2.1.10-1.0.29` **仅发布在 Maven Central**，阿里云 public/central/google 镜像均未同步（目录列表为空，单文件 200 但元数据不完整）。
- 在 `pluginManagement` 与 `dependencyResolutionManagement` 仓库列表追加 `mavenCentral()`。
- 进一步用 `CI` / `GITHUB_ACTIONS` 环境变量切换：CI 环境纯官方仓库（google / mavenCentral / gradlePluginPortal），本地开发用阿里云镜像加速。
- 涉及：`settings.gradle`、`build-logic/settings.gradle.kts`。

### 15. 修复 settings.gradle 语法错误（`ab0d649`）
- Gradle 要求 `pluginManagement {}` 必须是 settings 脚本的第一条语句。
- 将环境变量判断从脚本顶部移到 `pluginManagement { repositories { ... } }` 块内部。

### 16. 修复 CI 签名密钥加载失败（`08a2335` / `84cad64`）
- 原代码 `echo ${{ secrets.KEY_STORE }} | base64 --decode` 末尾会追加换行符，污染 jks 二进制，导致 BouncyCastle 报 `Tag number over 30 is not supported`。
- 改用 `printf '%s' '...' | base64 --decode`，并加 `file` / `ls -l` / 非空校验。
- 增加 `KEY_STORE` / `ALIAS_NAME` / `ALIAS_PASSWORD` / `KEY_PASSWORD` 四个 Secret 空值校验，避免白跑 14 分钟编译才发现签名失败。

### 17. 修复 Release 创建 403（`bbc7dda`）
- 原 `softprops/action-gh-release` 用 `secrets.TOKEN`（personal access token）权限不足。
- 改用默认 `secrets.GITHUB_TOKEN`，并给 workflow 添加 `permissions: contents: write`。

### 18. 版本号更新（`e2882fd` / `bbc7dda`）
- `1.26.062507` → `1.26.072910`（7月29日10点）。

---

## 五、最终 CI 构建结果

| 阶段 | 状态 |
|---|---|
| Gradle wrapper | ✅ |
| 仓库配置（Google / Maven Central / Plugin Portal） | ✅ |
| KSP 解析 | ✅ |
| settings.gradle 语法 | ✅ |
| 181 个编译 task 全部成功 | ✅ |
| 资源压缩 / R8 / DEX / APK 打包 | ✅ |
| 签名（4 个 Secret 配置正确） | ✅ |
| 上传 APK 到 Artifacts | ✅ |
| 创建 GitHub Release | ✅ |
| 上传 APK 到 Release | ✅ |

APK 下载：https://github.com/jonaskilli/tts-mumu/releases/tag/1.26.072910

---

## 附：7-27 遗留改动（已随 PR #1 / #2 合并入主干）

以下改动在 7-27 完成并通过 PR 合入 `mumu_tts_github`，本次一并记录在此：

### A. 音频参数界面统一下限（`7fdc7408`）
- 统一各音频参数（语速/音量/音调等）下限为 `0.1`，修正重置逻辑。
- 涉及：`BasicAudioParamsDialog`、`GroupAudioParamsDialog`、`LabelSlider`、`LocalTtsUI`、`PluginTtsUI`、`BgmConfigUI`、替换规则编辑等 15 个文件。

### B. 系统TTS分组列表支持多选（`bfbcc692`）
- 列表项支持多选（删除 / 转为子分组），操作入口移至顶栏。
- 涉及：`GroupItem`、`Group`、`ListManagerScreen` 等 4 个文件（+201 行）。

### C. 支持跨分组拖拽转移（`b79e5fde`）
- 分组可拖拽降级为子分组、配置项跨分组迁移。
- 涉及：`ListManagerScreen`、`ListManagerViewModel`（+128 行）。

### D. 按分组名一键分配标签（`f714b19b`）
- 按分组名一键分配标签，显示名统一为与朗读规则一致。
- 涉及：`Group`、`ListManagerScreen`。

### E. 显示名追加角色性格（`a9fb2def`）
- 当朗读规则含角色性格(personality)时，显示名追加该性格；未设置则不带。
- 涉及：`ListManagerScreen`。

### F. 批量分配标签显示名规则统一（`ca2ad829`）
- 老批量分配标签显示名统一规则：男主不补零、女主补零匹配。
- 涉及：`BatchTagDialog`、`ListManagerScreen`。

### G. 大分组整理 + 子分组关键词一键分配（`b04d0051`）
- 整理全部子分组；子分组支持按关键词一键分配；列表加滚动条。
- 涉及：`Group`、`ListManagerScreen`、`SubGroupHeader`。

### H. 插件配置新增仅界面模式（`637d1dde`）
- 插件 TTS 配置新增「仅界面模式」开关：隐藏试听、语言/发音人、语速音量音调等语音 chrome，仅显示插件自定义 UI。
- `PluginTtsSource` 新增 `isUiOnly` 字段。
- 涉及：`PluginTtsUI`、`PluginDescriptor`、`PluginTtsSource`、`strings.xml`。

### I. 新增工具箱页 + 导入自动识别（`c28271c1`）
- 底部导航新增「工具箱」页（第 2 栏），重排其余页索引。
- `MainPager` 开启左右滑动切换（`userScrollEnabled = true`）。
- 新增 `ToolBoxScreen`：自动列出所有开启「仅界面模式」的工具型插件，点开即渲染其自定义 UI。
- 导入文件按 JSON 结构自动识别类型（插件/朗读规则/替换规则/配置列表），识别不出再回退手动选择。
- 涉及：`PagerDestination`、`MainPager`、`ToolBoxScreen`、`ImportConfigActivity`、`ImportConfigFactory`、`strings.xml`。

### J. 转发器从底栏移入设置（`820f7742`）
- 转发器不再单独占用底栏一栏，改为「设置」页入口（底栏减为 5 栏）。
- 新增 `SystemTtsForwarderActivity` 承载原转发器界面并在 `AndroidManifest` 注册。
- 涉及：`PagerDestination`、`MainPager`、`MainSettingsScreen`、`AndroidManifest.xml`、新增 `SystemTtsForwarderActivity`、`strings.xml`。

### K. 转发器设置页实时开关（`0a6e986c`）
- 设置页「TTS转发器」条目右侧显示可见开关，副标题实时显示「运行中 / 已停止」。
- 通过 `LocalBroadcastReceiver` 监听 `ACTION_ON_STARTED` / `ACTION_ON_CLOSED` 广播同步状态。
- 直接点开关即可启停转发服务，点行其他区域打开详细配置页。
- 涉及：`MainSettingsScreen`、`strings.xml`。

---

## 当前底部导航（5 栏，支持左右滑动）

| 索引 | 页面 | 说明 |
|----|------|------|
| 0 | 系统TTS | 发音人配置列表 |
| 1 | 工具箱 | 仅界面模式的工具型插件 |
| 2 | 日志 | 运行日志 |
| 3 | 太极 | 混元太极代理 |
| 4 | 设置 | 含转发器开关入口 |

---

> 注：7-29 起所有改动均通过 GitHub Actions CI 实测构建成功（version `1.26.072910`），APK 已发布到 Release。
