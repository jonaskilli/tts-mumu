# 分组长按菜单功能说明

在配置列表中，每个大分组右侧有三个点（⋮）按钮：

- **单击**：显示原有菜单（重命名、复制、音频参数、排序、编辑内容、子分组操作、批量分配标签、导出、删除）
- **长按**：显示以下6个批量管理功能

---

## 1. 删除启用的配置

删除该分组下所有**已启用**的配置（含子分组内的）。

- 未启用的配置不受影响，保留在原位
- 删除后自动通知 TTS 服务刷新配置

## 2. 删除未启用的配置

删除该分组下所有**未启用**的配置（含子分组内的）。

- 已启用的配置不受影响
- 适用于清理不再使用的备用配置

## 3. 移动启用配置到其他分组

将该分组下所有**已启用**的配置移动到其他分组。

弹出选择对话框：
- 对话框可上下滚动，支持分组数量较多的情况
- 默认只显示大分组列表
- 每个大分组左侧有展开/合并箭头，点击可展开显示该大分组下的子分组
- 点击大分组名称 → 移动到该大分组根目录
- 点击子分组名称 → 移动到该大分组下的指定子分组
- 底部有"新建大分组"选项 → 输入名称创建新大分组并移动

移动后配置的排序位置追加到目标分组末尾。

## 4. 按原有序号重排标签（仅启用）

对该分组内的**已启用**配置，按标签序号重新排序。

排序规则：
- 按 `categoryPath`（子分组路径）分组，每个子分组内独立处理
- 同一子分组内，按标签前缀（汉字部分）再分组，不同前缀互不干扰
- 同前缀的启用配置：收集它们**当前已有的序号集合**，从小到大排序，按配置在分组中的位置顺序依次分配
- **序号集合不变**，只是哪个配置拿哪个序号重新对应

示例（某子分组内"女青年"标签的5个配置，按位置顺序）：
```
排序前：女青年05, 女青年03, 女青年08, 女青年01, 女青年06
原有序号集合 = {01, 03, 05, 06, 08}
排序后：女青年01, 女青年03, 女青年05, 女青年06, 女青年08
```
> 序号仍为 01/03/05/06/08，不是从01连续，只是按位置重新对应。

未启用的配置不参与，标签保持不变。

## 5. 从01重排标签（仅启用）

对该分组内的**已启用**配置，按标签序号从01开始重新连续编号。

排序规则：
- 按 `categoryPath` 分组，每个子分组内独立处理
- 同前缀的启用配置：按配置在分组中的位置顺序，从 **01** 开始连续编号（01, 02, 03...）
- 不同前缀互不干扰

示例（某子分组内"女青年"标签的5个配置，按位置顺序）：
```
排序前：女青年05, 女青年03, 女青年08, 女青年01, 女青年06
排序后：女青年01, 女青年02, 女青年03, 女青年04, 女青年05
```
> 序号从01开始连续，忽略原有的序号值。

未启用的配置不参与，标签保持不变。

## 6. 重新分配标签（输入前缀）

弹出一个输入框，输入标签前缀（如"少女"），将该分组内所有**已启用**配置的标签统一替换为指定前缀+两位序号。

规则：
- 按 `categoryPath` 分组，每个子分组内独立处理
- 每个子分组内的启用配置按当前位置顺序，从 `前缀01` 开始连续编号
- 标签名和标签显示名同时更新

示例（输入"少女"，某子分组内有3个启用配置）：
```
配置A → 少女01
配置B → 少女02
配置C → 少女03
```

> 注意：此功能会**覆盖**原有标签，不管原来是什么标签，全部替换为输入的前缀+序号。适用于将一批配置统一归为同一分类。

未启用的配置不参与，标签保持不变。
