# 代码折叠与搜索功能技术文档

> 本文档详细记录 TTS Server Android 应用中代码编辑器的**折叠**、**展开**、**复制折叠代码**、**搜索**功能的实现细节，以及开发过程中遇到的关键问题和解决方案。

---

## 目录

1. [技术背景](#1-技术背景)
2. [文件结构](#2-文件结构)
3. [折叠机制：去换行方案](#3-折叠机制去换行方案)
4. [折叠时的注释处理](#4-折叠时的注释处理)
5. [展开机制与行号同步](#5-展开机制与行号同步)
6. [展开时保持屏幕位置不变](#6-展开时保持屏幕位置不变)
7. [折叠按钮 Overlay 绘制](#7-折叠按钮-overlay-绘制)
8. [点击交互逻辑](#8-点击交互逻辑)
9. [布局测量：解决空白问题](#9-布局测量解决空白问题)
10. [复制折叠代码](#10-复制折叠代码)
11. [搜索功能](#11-搜索功能)
12. [关键问题与踩坑记录](#12-关键问题与踩坑记录)

---

## 1. 技术背景

### sora-editor 无原生折叠 API

项目使用的代码编辑器是 [sora-editor (Rosemoe)](https://github.com/Rosemoe/sora-editor) 0.23.5 版本。**该版本不提供原生代码折叠 API**，没有 `fold`/`unfold` 方法，没有 `FoldRegion` 类，没有折叠按钮渲染机制。

因此整个折叠功能是从零实现的，采用**"去换行"方案**——通过修改编辑器文本来模拟折叠效果。

### 为什么不用原生方案

sora-editor 的设计更偏向于高性能文本编辑，折叠功能需要：
- 在渲染层跳过折叠的行
- 保持行号连续
- 支持嵌套折叠

这些都需要侵入式修改 sora-editor 源码，维护成本高。去换行方案虽然会修改文本，但实现简单、信息无损、可完美还原。

---

## 2. 文件结构

```
app/src/main/java/com/github/jing332/tts_server_android/compose/codeeditor/
├── CodeEditorScreen.kt      # 编辑器主界面（工具栏、搜索栏、布局、复制按钮）
├── CodeEditor.kt            # sora-editor 的 Compose AndroidView 封装
├── CodeFoldingManager.kt    # 折叠核心逻辑（折叠/展开/行号同步/复制还原）
└── CodeFoldingOverlay.kt    # 折叠按钮绘制层（Canvas overlay + 点击处理）
```

---

## 3. 折叠机制：去换行方案

### 核心思想

把一个代码块（由 `{}` 或 `[]` 包裹）的**中间所有行的换行符替换为空格**，合并成一行长代码。保留首行和尾行不动，这样：

- 折叠前：N 行代码
- 折叠后：3 行（首行 + 合并的中间行 + 尾行）
- 信息无损：原始内容完整保存，展开时 100% 还原

### FoldRegion 数据结构

```kotlin
data class FoldRegion(
    var currentStart: Int,       // 当前行号（动态更新，0-based，含）
    var currentEnd: Int,         // 当前行号（动态更新，0-based，含），折叠后 = currentStart + 2
    val originalFirst: String,   // 原始首行内容
    val originalMiddle: String,  // 原始中间行内容（含换行，按原样保存）
    val originalLast: String,    // 原始尾行内容（不含"折叠了N行"注释）
    val collapsedMiddle: String, // 折叠后的中间合并行（去换行用空格连接）
)
```

**关键设计：`currentStart`/`currentEnd` 是动态更新的**

每次 fold/unfold 后，后续所有折叠区域的行号都要同步更新。不能用"折叠前的原始行号"做定位，否则会导致行号越界崩溃（这是早期版本的严重 bug）。

### 可折叠块识别

`findFoldableBlocks()` 用**带类型的栈**分析括号匹配：

```kotlin
val stack = ArrayDeque<Pair<Int, Char>>()  // Pair(行号, 括号类型 '{' 或 '[')
```

- 遍历每行每个字符
- 遇到 `{` 或 `[` → 入栈
- 遇到 `}` 或 `]` → 弹出栈直到匹配到对应类型的开括号
- 只折叠跨越 ≥3 行的块（首行 + 至少1中间行 + 尾行）
- **字符串感知**：字符串内的括号不参与匹配（简易处理，跟踪 `"` `'` 状态）

支持两种折叠类型：
- `{}` — 对象/函数体
- `[]` — 数组

支持嵌套（内层块也可以独立折叠）。

### fold() 折叠流程

```
输入: startLine, endLine (0-based, 含)

1. 保存原始中间行 (startLine+1 到 endLine-1)
   originalMiddle = middleLines.joinToString("\n")

2. 构建折叠后的中间行
   collapsedMiddle = middleLines
       .map { stripLineComment(it.trim()) }  // 去掉行内注释
       .filter { it.isNotEmpty() }           // 丢弃空行和纯注释行
       .joinToString(" ")                    // 用空格连接

3. 构建替换内容
   newContent = firstLine + "\n" + collapsedMiddle + "\n" + lastLineWithHint
   // lastLineWithHint = "$lastLine  // 折叠了${foldedCount}行"

4. 用 sora-editor 文本替换
   text.replace(start, end, newContent)

5. 记录 FoldRegion
   currentStart = startLine
   currentEnd = startLine + 2  (首行+合并行+尾行 = 3行)

6. 同步后续折叠区域行号（前移 reducedLines）
```

**为什么用空格连接而不是直接拼接？**
JS 语法要求语句间有分隔符。直接拼接 `foo()bar()` 会语法错误，用空格连接 `foo() bar()` 保持合法。

---

## 4. 折叠时的注释处理

### 问题

折叠时把多行合并成一行，如果中间有 `//` 单行注释，注释会把后面的代码都"吞掉"：

```javascript
// 折叠前：
let x = 1; // 设置x
let y = 2;

// 如果直接去换行合并：
let x = 1; // 设置x let y = 2;
// → "let y = 2;" 被注释吞掉了！
```

### 解决方案：stripLineComment()

折叠时对每行调用 `stripLineComment()` 去掉注释部分：

```kotlin
private fun stripLineComment(line: String): String {
    var inString = false
    var stringChar = ' '
    var i = 0
    while (i < line.length - 1) {
        val c = line[i]
        if (inString) {
            if (c == stringChar && line[i - 1] != '\\') inString = false
        } else {
            when {
                c == '"' || c == '\'' -> { inString = true; stringChar = c }
                c == '/' && line[i + 1] == '/' -> return line.substring(0, i).trim()
            }
        }
        i++
    }
    return line.trim()
}
```

处理三种情况：
- 整行注释 `// xxx` → 返回 `""`（被 filter 丢弃）
- 行内注释 `code; // xxx` → 返回 `"code;"`
- 无注释 `code;` → 返回 `"code;"`

**字符串感知**：字符串内的 `//` 不误删，例如：
```javascript
var url = "http://example.com"  // 这里的 // 在字符串内，不截断
```

### 重要说明

注释只是折叠**显示**时去掉，原始内容完整保存在 `originalMiddle` 中，展开时 100% 还原，注释不会真的丢失。

---

## 5. 展开机制与行号同步

### unfold() 展开流程

```
输入: region (FoldRegion)

1. 构建还原内容（用保存的原始内容）
   originalContent = region.originalFirst + "\n" + region.originalMiddle + "\n" + region.originalLast

2. 用 sora-editor 文本替换
   text.replace(start, end, originalContent)

3. 计算增加的行数
   addedLines = region.originalMiddle.count { it == '\n' }

4. 从 regions 列表移除该区域

5. 同步后续折叠区域行号（后移 addedLines）
```

**关键点**：展开时用的是 `originalFirst/originalMiddle/originalLast`，不是当前编辑器文本。因为折叠后尾行被加了 `// 折叠了N行` 注释，必须用保存的原始尾行还原。

### 行号同步算法

每次 fold/unfold 都要更新后续所有区域的 `currentStart`/`currentEnd`：

```
fold 时：后续区域行号 -= reducedLines (减少的行数)
unfold 时：后续区域行号 += addedLines (增加的行数)
```

这是避免**展开崩溃**的关键。早期版本用"原始行号"做定位，折叠后行号变了但记录没更新，导致 `text.getCharIndex(line, 0)` 越界崩溃。

### foldAll / unfoldAll

- `foldAll()`：从后往前折叠所有块（避免行号影响）
- `unfoldAll()`：从后往前展开（每步位置都准确）

从后往前是关键：前面的块折叠/展开不影响后面块的行号。

---

## 6. 展开时保持屏幕位置不变

### 问题

点击展开按钮时，`text.replace()` 会把光标和滚动位置都移到**替换内容的末尾**（即展开后最后一行）。即使之后把光标移回首行，屏幕也已经跳转了，造成视觉割裂：

```
折叠状态：
  [屏幕中间] function foo() { ▶
  
点击展开后：
  [屏幕顶部] function foo() {     ← 原来在中间，现在跳到顶部了
              let x = 1;
              ...
            }
```

### 解决方案：保存并恢复 offsetY

```kotlin
// handleClick 中展开逻辑
for (region in foldingManager.getRegions()) {
    val line = region.currentStart
    if (clickContentY in y..(y + rowHeight)) {
        // 1. 展开前记住滚动偏移
        val savedOffsetY = editor.offsetY
        
        // 2. 执行展开（text.replace 会导致滚动跳转）
        editor.setSelection(line, 0, true)
        foldingManager.unfoldAt(line)
        
        // 3. 用 scroller 瞬间恢复滚动位置
        runCatching {
            val scroller = editor.scroller
            // startScroll(startX, startY, dx, dy, duration)
            // 从 savedOffsetY 开始 0 距离 0 时长滚动 = 瞬间设置滚动位置
            scroller.startScroll(0, savedOffsetY, 0, 0, 0)
            scroller.forceFinished(true)
        }
        
        // 4. 光标定位到首行开头
        editor.setSelection(line, 0, true)
        editor.invalidate()
        return
    }
}
```

### 原理

sora-editor 的 `getOffsetY()` 返回 `EditorScroller.getCurrY()`。`EditorScroller` 内部持有 `OverScroller`。

`startScroll(startX, startY, dx, dy, duration)` 调用 `OverScroller.startScroll` 然后调用 `setEditorOffsets()` 把 scroller 位置同步到编辑器。

当 `dx=0, dy=0, duration=0` 时，等于瞬间设置滚动位置到 `startY`，配合 `forceFinished(true)` 立即完成，不会产生动画。

**效果**：展开后屏幕滚动位置保持不变，原来在屏幕中间的折叠行仍然停在中间，只是下方代码向下拓展显示出来。

---

## 7. 折叠按钮 Overlay 绘制

### 架构

`CodeFoldingOverlay` 是一个 Compose `Canvas`，叠加在编辑器的**行号区（gutter）**上：

- 只覆盖左侧 56dp 宽度（gutter），不挡代码区
- 不影响光标操作和滚动
- 通过轮询 `editor.offsetY` 和 `lineCount` 触发重绘

### 按钮样式

| 状态 | 图标 | 颜色 | 含义 |
|------|------|------|------|
| 可折叠未折叠 | ▼ 下指三角 | `#FFFF6B35` 橙色 | 点击折叠 |
| 已折叠 | ▶ 右指三角 | `#FF4A90D9` 蓝色 | 点击展开 |

折叠行背景：`#404A90D9`（alpha=0x40，约25%透明度蓝色），覆盖首行+合并行+尾行共3行。

按钮尺寸：`rowHeight * 0.32`（halfSize），比初始版本大，更易点击。

### 绘制流程

```kotlin
LaunchedEffect(editor) {
    // 每 50ms 轮询 offsetY 和 lineCount，变化时 redrawTrigger++
}

Canvas(
    modifier = modifier
        .width(56.dp)
        .fillMaxHeight()
        .pointerInput(redrawTrigger) {
            detectTapGestures { offset -> handleClick(...) }
        }
) {
    drawFoldIndicators(editor, foldingManager, ...)
}
```

`drawFoldIndicators` 做两件事：
1. 遍历已折叠区域，绘制蓝色背景 + ▶ 按钮
2. 遍历可折叠块（排除已折叠的），绘制 ▼ 按钮

### Y 坐标计算

```kotlin
private fun getLineY(editor: CodeEditor, line: Int): Float {
    return try {
        val offsets = editor.layout.getCharLayoutOffset(line, 0)
        // [0] 是字符底部位置，减去行高得到行顶部
        offsets[0] - editor.rowHeight.toFloat()
    } catch (e: Exception) {
        (editor.rowHeight * line).toFloat()  // fallback
    }
}
```

用 sora-editor 的 `layout.getCharLayoutOffset` 获取字符位置，自动兼容 wordwrap 与非 wordwrap 模式。

绘制时减去 `offsetY`（滚动偏移）转换为屏幕坐标。

---

## 8. 点击交互逻辑

`handleClick()` 处理 overlay 上的点击，按优先级匹配：

### 1. 点击已折叠区域 → 展开

```kotlin
val clickContentY = offset.y + offsetY  // 屏幕坐标转内容坐标

for (region in foldingManager.getRegions()) {
    val y = getLineY(editor, region.currentStart)
    if (clickContentY in y..(y + rowHeight)) {
        // 命中折叠行 → 展开（见第6节）
    }
}
```

### 2. 点击可折叠未折叠区域 → 折叠

```kotlin
val blocks = foldingManager.findFoldableBlocks()
for (block in blocks) {
    if (foldingManager.isFolded(block.first)) continue  // 跳过已折叠
    val y = getLineY(editor, block.first)
    if (clickContentY in y..(y + rowHeight)) {
        editor.setSelection(line, 0, true)  // 先移光标
        foldingManager.fold(block.first, block.last)
        return
    }
}
```

**为什么折叠前要先 `setSelection`？**
如果不先移光标，折叠后 sora-editor 可能会把屏幕跳回原光标位置，导致刚折叠的行滚出视野。

### 3. 点击行号区其他位置 → 光标移到该行开头

```kotlin
for (line in firstVisibleLine..lastVisibleLine) {
    val y = getLineY(editor, line)
    if (clickContentY in y..(y + rowHeight)) {
        editor.setSelection(line, 0, true)
        return
    }
}
```

### 4. 点击可见行之外 → 定位到最近的可见行

---

## 9. 布局测量：解决空白问题

### 问题现象

工具栏下方出现大量空白，**空白高度与代码行数成正比**：
- 朗读规则页（52行代码）→ 空白很小
- 插件编辑页（几百行代码）→ 空白很大

### 根因

sora-editor 的 `onMeasure` 在 `AT_MOST` 约束模式下，返回 `rowHeight × lineCount` 作为高度。代码越多，返回的高度越大，导致编辑器被撑高，上方出现空白。

### 解决方案：自定义 Layout + EXACTLY 约束

```kotlin
androidx.compose.ui.layout.Layout(
    modifier = Modifier.weight(1f).fillMaxWidth(),
    content = {
        CodeEditor(...)           // 第一个子：编辑器
        CodeFoldingOverlay(...)   // 第二个子：折叠按钮层
    }
) { measurables, constraints ->
    // 强制给 CodeEditor 传 EXACTLY 约束
    // minWidth = minHeight = maxWidth = maxHeight
    // 阻止 sora-editor 按文本行数返回高度
    val fixedConstraints = Constraints(
        minWidth = constraints.maxWidth,
        minHeight = constraints.maxHeight,
        maxWidth = constraints.maxWidth,
        maxHeight = constraints.maxHeight
    )
    val editorPlaceable = measurables[0].measure(fixedConstraints)
    
    // overlay 用编辑器实际尺寸测量
    val overlayPlaceable = measurables[1].measure(
        Constraints(0, 0, editorPlaceable.width, editorPlaceable.height)
    )
    
    layout(editorPlaceable.width, editorPlaceable.height) {
        editorPlaceable.place(0, 0)
        overlayPlaceable?.place(0, 0)
    }
}
```

**关键点**：
1. 自定义 `Layout` 的 `weight(1f)` 获得固定空间
2. 测量时给 `CodeEditor`（AndroidView）传 **EXACTLY** 约束（min=max），sora-editor 必须使用这个固定高度
3. overlay 在 Layout **内部**测量，不影响外部高度

### 为什么用自定义 Layout 而不是 Box？

`Box` 包裹 `CodeEditor(fillMaxSize)` + overlay 时，`AndroidView` 的 intrinsic measurement 会参与，sora-editor 的 `onMeasure` 仍可能收到 `AT_MOST` 并按内容返回高度。自定义 Layout 可以完全控制传给子组件的约束。

---

## 10. 复制折叠代码

### 需求

用户选中代码（可能跨越折叠区域）后点复制按钮，剪贴板里应该是**展开后的原始代码**，不是折叠后的合并行。

### 实现：getUnfoldedSelectionText()

```kotlin
fun getUnfoldedSelectionText(startLine, startCol, endLine, endCol): String {
    var line = startLine
    while (line <= endLine) {
        val region = regions.firstOrNull { line in it.currentStart..it.currentEnd }
        
        val content = if (region != null) {
            // 当前行在折叠区域内 → 还原原始内容
            when (line) {
                region.currentStart -> region.originalFirst      // 首行
                region.currentStart + 1 -> region.originalMiddle // 合并行→原始多行
                else -> region.originalLast                       // 尾行
            }
        } else {
            // 普通行 → 取当前文本
            text.getLineString(line)
        }
        
        // 处理首尾行的列偏移
        // ...
        result.append(content)
        line++
    }
    return result.toString()
}
```

### 格式转换说明

| 选区位置 | 编辑器显示 | 复制到剪贴板 |
|---------|-----------|-------------|
| 普通行 | `let x = 1;` | `let x = 1;`（原样） |
| 折叠区首行 | `function foo() {` | `function foo() {`（originalFirst） |
| 折叠区合并行 | `let a=1; let b=2; ...` | `let a = 1;\nlet b = 2;\n...`（originalMiddle，含换行） |
| 折叠区尾行 | `}  // 折叠了5行` | `}`（originalLast，不含注释） |

### 工具栏复制按钮

```kotlin
IconButton(onClick = {
    codeEditor?.let { editor ->
        val cursor = editor.cursor
        if (!cursor.isSelected()) {
            context.longToast("请先选择要复制的代码")
            return@let
        }
        val text = if (foldingManager != null) {
            foldingManager.getUnfoldedSelectionText(
                cursor.leftLine, cursor.leftColumn,
                cursor.rightLine, cursor.rightColumn
            )
        } else {
            editor.text.subSequence(start, end).toString()
        }
        cb.setPrimaryClip(ClipData.newPlainText("code", text))
        context.longToast("已复制${text.lineSequence().count()}行")
    }
})
```

**注意**：`cursor.leftLine`/`cursor.rightLine` 在 Kotlin 中是**属性**不是函数，用 `cursor.leftLine` 不是 `cursor.leftLine()`。

---

## 11. 搜索功能

### 架构

搜索功能内置在 `CodeEditorScreen` 中，朗读规则页和插件编辑页共用。通过 `showSearchBar` 状态控制显示/隐藏。

### 搜索栏 UI

```
[输入框..............] [搜索] [上一个] [下一个] [关闭]
```

位于 TopAppBar 下方，CodeEditor 上方。

### 搜索流程

```kotlin
// 1. 用户输入关键词，点搜索按钮
searcher.search(searchKeyword, EditorSearcher.SearchOptions(false, false))
// SearchOptions(caseSensitive=false, regex=false)

// 2. 延迟100ms后跳到第一个匹配
codeEditor.postDelayed({
    if (searcher.hasQuery()) {
        runCatching { searcher.gotoNext() }
    }
}, 100)

// 3. 上一个/下一个
searcher.gotoPrevious()
searcher.gotoNext()

// 4. 关闭搜索
searcher.stopSearch()
```

### 关键点

- `searcher.search()` 是异步的，需要 `postDelayed` 延迟跳转
- `gotoNext()`/`gotoPrevious()` 可能抛异常（无匹配时），用 `runCatching` 包裹
- 清除关键词时调用 `searcher.stopSearch()` 清除高亮
- 搜索栏可通过菜单项"搜索"切换显示，也可点关闭按钮隐藏

### 朗读规则页 vs 插件页

两个页面都调用同一个 `CodeEditorScreen`，搜索功能自动可用。无需各自实现。

---

## 12. 关键问题与踩坑记录

### 12.1 sora-editor 无原生折叠

**问题**：一开始尝试用 sora-editor 原生折叠 API，发现根本不存在。

**解决**：查看 sora-editor 源码和 README 确认无折叠功能，改为"去换行"方案。

### 12.2 展开崩溃（行号越界）

**问题**：展开折叠区域时 `text.getCharIndex(line, 0)` 崩溃，行号越界。

**根因**：折叠后行号变了，但用"折叠前原始行号"做定位。

**解决**：`FoldRegion` 改用 `currentStart`/`currentEnd` 动态行号，每次 fold/unfold 同步更新后续区域。

### 12.3 注释吞代码

**问题**：折叠含 `//` 注释的代码时，注释把后续合并行都吞掉。

**解决**：`stripLineComment()` 带字符串感知地截掉 `//` 注释。注意区分整行注释和行内注释，字符串内的 `//` 不误删。

### 12.4 Overlay 阻挡光标和滚动

**问题**：overlay 用 `fillMaxSize` + `pointerInput` 覆盖整个编辑器，导致光标无法点击、无法滚动。

**解决**：overlay 只覆盖 56dp gutter 宽度，用 `detectTapGestures`（不是 `pointerInput` 的其他手势）处理点击。

### 12.5 工具栏下方空白

**问题**：代码越多，工具栏下方空白越大。

**根因**：sora-editor `onMeasure` 在 `AT_MOST` 模式返回 `rowHeight × lineCount`。

**解决**：自定义 Layout 给 CodeEditor 传 EXACTLY 约束（min=max）。**不能移除 overlay 的 `fillMaxHeight()`**，否则 Canvas 高度为 0 按钮不显示。

### 12.6 展开时光标跳到末尾

**问题**：点击展开后光标跳到展开内容最后一行末尾。

**根因**：`text.replace()` 会把光标移到替换内容末尾。

**解决**：展开后再次 `setSelection(line, 0, true)` 重新定位到首行开头。

### 12.7 展开时屏幕位置跳动

**问题**：展开时屏幕先跳到末尾再跳回首行，原在中间的折叠行跑到顶部。

**根因**：`text.replace()` 同时改变了滚动位置。

**解决**：展开前记录 `offsetY`，展开后用 `scroller.startScroll(0, savedOffsetY, 0, 0, 0)` + `forceFinished(true)` 瞬间恢复滚动位置。

### 12.8 cursor 属性访问语法

**问题**：编译报错 `Expression 'rightLine' of type 'kotlin.Int' cannot be invoked as a function`。

**根因**：sora-editor 的 `Cursor.getLeftLine()` 在 Kotlin 中映射为属性 `cursor.leftLine`，不是函数 `cursor.leftLine()`。

**解决**：用 `cursor.leftLine`、`cursor.rightLine` 属性访问语法。

### 12.9 折叠行底色太浅

**问题**：折叠行底色 `0x154A90D9`（alpha=8%）太浅看不出。

**解决**：调深到 `0x404A90D9`（alpha=25%）。

---

## 附录：API 速查

### sora-editor CodeEditor 常用 API

```kotlin
editor.text                    // Content 文本对象
editor.text.replace(start, end, newContent)  // 文本替换
editor.text.getCharIndex(line, col)          // 行列转 char index
editor.text.getLineString(line)              // 获取整行文本
editor.text.getLineCount                     // 行数

editor.cursor                   // Cursor 对象
editor.cursor.isSelected()      // 是否有选区
editor.cursor.leftLine          // 选区左端行（属性，不是函数）
editor.cursor.leftColumn        // 选区左端列
editor.cursor.rightLine         // 选区右端行
editor.cursor.rightColumn       // 选区右端列

editor.setSelection(line, col, leavePreviousSelection)  // 设置光标
editor.offsetY                  // 当前垂直滚动偏移（像素）
editor.scroller                 // EditorScroller
editor.scroller.startScroll(x, y, dx, dy, duration)  // 滚动
editor.scroller.forceFinished(true)                   // 强制完成滚动
editor.rowHeight                // 行高（像素）
editor.firstVisibleLine         // 第一个可见行
editor.lastVisibleLine          // 最后一个可见行
editor.layout.getCharLayoutOffset(line, col)  // 获取字符位置 [x, y]

editor.searcher                 // EditorSearcher
editor.searcher.search(keyword, options)      // 搜索
editor.searcher.gotoNext()                   // 下一个
editor.searcher.gotoPrevious()               // 上一个
editor.searcher.stopSearch()                  // 停止搜索
```

### Compose 自定义 Layout

```kotlin
Layout(
    modifier = Modifier.weight(1f).fillMaxWidth(),
    content = { /* 子组件 */ }
) { measurables, constraints ->
    // constraints: Constraints(minWidth, maxWidth, minHeight, maxHeight)
    // EXACTLY: min == max
    // AT_MOST: min < max
    
    val placeable = measurables[0].measure(fixedConstraints)
    layout(width, height) {
        placeable.place(x, y)
    }
}
```
