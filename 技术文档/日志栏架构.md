# 日志栏架构

App 主界面第 2 个 Tab（index=1，`PagerDestination.SystemTtsLog`）是日志栏，展示系统 TTS 运行过程中的各类日志。本文档记录日志栏的汇聚机制、接入方式与过滤逻辑，供后续接入新日志源参考。

## 一、整体架构

```
日志栏 UI (TtsLogScreen, index=1 Tab)
   │
   ├── TtsLogViewModel.logs (内存列表 mutableStateListOf<LogEntry>)
   │     │
   │     ├── pull() ← cache/log/system_tts.log (启动恢复最近1500行)
   │     │
   │     ├── 源1: SysttsLogger.register { addLog(it) }
   │     │      ├── SystemTtsService.logS() 直接调用 (+写文件)
   │     │      └── SysttsFilter (Logback过滤器, 拦截 SystemTtsService.TAG + 指定 logger 名)
   │     │
   │     ├── 源2: Console.globalPluginLogListener = { addLog(it) }
   │     │      ← 各 Console(PLUGIN) 实例的 write() 触发
   │     │
   │     └── 源3: Console.globalSpeechRuleLogListener = { addLog(it) }
   │            ← Console(SPEECH_RULE) 实例的 write() 触发
   │
   ├── filteredLogs (级别 + 搜索词 + 插件开关 + 朗读规则开关)
   │
   └── LogScreen (LazyColumn 渲染, HTML消息着色, 自动滚动)
```

**核心设计**：三个全局监听器是汇聚点。所有日志源无论创建多少实例，都通过这三个汇聚点进入唯一的 `TtsLogViewModel.logs`，再经过滤后渲染到日志栏。

## 二、三个汇聚点

### 源1：SysttsLogger（原生 Kotlin 代码的日志）

- **定义**：`app/.../SysttsLogger.kt`，单例 object，内部用 `CopyOnWriteArraySet<LogListener>` 维护监听器
- **写入方式**：
  - 直接调用：`SysttsLogger.log(LogEntry(...))`，如 `SystemTtsService.logS()`（SUCCESS 级别，同时写文件持久化）
  - Logback 过滤器：`SysttsFilter`（继承 logback `Filter<ILoggingEvent>`），拦截指定 logger 名的事件，转成 `LogEntry` 投递给 SysttsLogger
- **白名单 logger**（`SysttsFilter`）：
  - `SystemTtsService.TAG`（系统 TTS 主日志）
  - `PLUGIN_LOGGER_NAMES`（插件相关，标记 `isPluginLog=true`）
  - `SYSTTS_INTERNAL_LOGGER_NAMES`（系统 TTS 内部模块，如 `SpeakerLoudnessManager`，`isPluginLog=false`）

### 源2 & 3：Console（脚本/JS 的日志）

- **定义**：`lib-script/.../console/Console.kt`，每个实例带 `source: LogSource`（PLUGIN 或 SPEECH_RULE）
- **机制**：`write(level, str)` 方法根据 source 调用对应的全局静态监听器：
  - PLUGIN → `Console.globalPluginLogListener`
  - SPEECH_RULE → `Console.globalSpeechRuleLogListener`
- **监听器赋值**：由 `TtsLogViewModel.init` 统一注册（`Console.globalXxxLogListener = { addLog(it) }`）
- **Console 实例创建点**：
  - 朗读规则引擎：`TextProcessor.kt` 创建 `Console(LogSource.SPEECH_RULE)`
  - 朗读规则编辑页：`SpeechRuleEditViewModel.kt`
  - 插件编辑页：`PluginEditorViewModel.kt`
  - 脚本运行时默认：`RhinoScriptRuntime.kt`（默认 PLUGIN）

## 三、接入新日志源

### 方式 A：原生 Kotlin 代码（走 SysttsLogger + SysttsFilter）

适用于 lib-tts / app 模块的 Kotlin 代码。

1. 在产生日志处用 `KotlinLogging.logger("LoggerName")` 创建 logger，调用 `logger.info { "..." }` 等
2. 在 `SysttsFilter.kt` 的 `SYSTTS_INTERNAL_LOGGER_NAMES`（非插件）或 `PLUGIN_LOGGER_NAMES`（插件）加入 logger 名
3. 日志经 logback → SysttsFilter 拦截 → SysttsLogger → 日志栏，自动显示

> 注意：lib-tts 不能直接引用 app 模块的 SysttsLogger，但走 Logback 过滤器无需直接引用——只要 logger 名在白名单里，SysttsFilter 会自动拦截转发。

### 方式 B：新增独立日志类别（带独立开关）

如果需要独立的显示开关（不混在插件/朗读规则日志里）：

1. `LogEntry.kt`（lib-common）新增 `isXxxLog: Boolean = false` 字段
2. `Console.kt` 新增 `LogSource` 枚举值 + 全局监听器字段 + write() 分支（若走 Console 机制）
### 方式 C：日志快捷面板（LogQuickPanel）

点击带 `configId` 的“请求音频”主行弹出的 AppDialog 居中弹窗（用户 09-09 定稿），结构三块：

1. **当前发音人**（顶部共用）：人名 + ▶ 试听（■/… 状态机同候选行）+ 终值行（三层乘积，1.0 维度不显示）
2. **SegmentedTextToggle 两区**（同编辑页「朗读全部/标签」样式，宽度适配文字无对勾）：
   - [更换发音人]
     - 对话绑定模式（多角色/本地音效槽位）：分类下拉（全部/女青年/男青年/女中年/男老年/少女/少年/女童/男童/女主/男主/特殊女/特殊男，带（N项）计数，0 项分类隐藏）+ 搜索 + 候选列表（行内试听 ▶/…/■ 状态机，点行=● 暂存，底部确认落库）。**匹配口径终版=tag（id）**：候选池=fayinren.json ∩ 启用配置的 tag 集合，tagName 是显示名不参与匹配（09-12 四轮反转定稿，改匹配前必读规则 JS +《字段关联架构》）；rebind 写 characterRecords.json 并**同步写 gengxin.json**（规则每轮朗读开头消费该信箱，否则旧内存会覆盖面板修改）
     - **本地音效槽位候选例外（09-13 定，目目拍板"只列本地音效槽位"）**：这类槽位**不能**读发音人池——池子 `fayinren.json` 由规则 `detectAvailableVoices` 生成，它只遍历 `GENSHIN_CHARACTERS`（音色标签），`localSound` 前缀不在其中，**音效标签永远进不了池子**（09-13 目目截图实锤：本地音效槽位列出 799 个 TTS 音色，只有补在顶部的当前项是音效）。改为在**启用配置**里枚举同族槽位 `tag=localSoundN`（正则 `^localSound\d+$`），语义="本槽位可借用哪些音效槽位的配置"；候选行与 Toast 把 `localSoundN` 显示成 `本地音效N`（与规则 tags 表、列表角标同口径）。换绑仍写 `characterRecords[本地音效N]=localSoundM`；**音效槽位不显示分类下拉/搜索框，候选行也不显示 ▶ 试听键**（09-13 追加：音效标签不入音色分类 → 下拉只剩「全部（N项）」一项，毫无意义；试听喂固定句「你好，这是试听语音。」而音效插件按正则匹配文本取音效，多半匹配不上不出声，索性藏键）。
     - 旁白/非绑定（09-12 推广定稿）：按**本配置项的 tag（id）**列同标签候选（旁白/对话/括号/本地音效天然归组，无特殊分支），点行=● 暂存，底部确认写配置项 voice，**落库后经 SharedViewModel.pendingLocateConfigId 通知主列表：展开所在分组、滚动到位、2.5s 高亮**
   - [音频参数]：**按维度分段（09-10 二稿）**——「语速 / 音量 / 音高」第二级 SegmentedTextToggle（共用组件 `AudioParamsDimensionSection`，与配置项音频参数弹窗同款），每维内 配置→插件→全局 三层滑杆同屏；重置/应用**按维度一组**（应用=该维三层一起落库，插件层写目标插件+失效卡片缓存、全局层写 SysTtsConfig，插件接管 pluginHandlesX 的维度只落配置层，且插件/全局滑杆隐藏换「该插件自行处理 X」说明行）；音高进插件/全局层（翻掉 09-07 旧决定）。三层现值总览行已撤（09-10 ④），顶部终值行足够。该行与卡片参数行、音频参数弹窗、编辑页内嵌终值行**四处完全统一**（09-10 二稿 B 案）：`最终：语速1.0，音量1.85，音高1.2`——「最终：」前缀 + 全角逗号分隔 + 不加粗 + **按实际精度**（`Float.toParamText()`：1.00→1.0、1.50→1.5、0.97→0.97，最多 2 位、末尾无效 0 去掉但至少留 1 位）；三维恒显，不再按 ≠1.0 过滤，无「无设置」占位。此前管道分隔+加粗+1 位小数的差异化样式已撤（1.04 在卡片显示成 1.0 会被误读为未设置）。试听弹窗（AuditionDialog）与日志发音人信息（SystemTtsService）同样删 x 后缀、走 toParamText，试听项间由空格改全角逗号。面板内容高度上限跟随屏幕（85%）而非固定 600dp。底部无取消/确定行，补 4dp 底边距收尾。
   - **参数跟随所选发音人（09-10）**：暂存/确认候选后，音频参数区、终值行、顶部试听全部切换为该发音人对应配置项的参数（绑定模式=绑定(含暂存)标签的启用配置项；旁白=暂存候选的配置项；无暂存=本配置项），目标切换时三层草稿+插件元数据整体重载、脏标记复位；旁白确认时 voice+当前参数草稿一并写入本配置项（确认前后所见即所得）；顶部「当前发音人」名同步跟随暂存显示。
3. **换声两段式**（用户 09-08 定稿）：点候选行=暂存选中（●），底部「确认」才落库；未确认关闭即弃。绑定模式写 `characterRecords.json`，旁白写配置项 `voice`。
4. **候选行标记与删除（09-12 晚，与角色管理 v10 互通）**：绑定模式候选行内平铺——❤️🚶😈 三标记（IconButton，点亮=实色/未点亮=0.35 alpha，emoji 彩色字形染不上色故用 alpha；多选 toggle，写 `voice_marks.json`，app 侧通道 `VoiceMarksFile.kt`，文件在 `Download/chajian/<tagRuleId>/` 与 fayinren/characterRecords 同目录）+ 🗑 删除（只删 ▶ 试听指向的**那一条**启用配置——用户 09-12 纠正"听到啥删啥"，不按标签放大；仅当标签删空才从 fayinren.json 移除；有确认弹窗；app 侧 `CharacterRecordsFile.removeFromPool`）。配套修了 v10 `loadVoiceMarks` 的会话级缓存早退（面板也会写该文件，缓存不失效会把面板标记覆盖掉）。**待装机实测**：目目反馈"列表没空间放那么多 emoji"，平铺布局可能要改紧凑方案（收窄/两行/回收菜单）。

---

## 三、日志落盘格式（MDC 五段）

09-08 引入 MDC（Mapped Diagnostic Context）将 `configId` 与 `roleName` 落盘，重启后仍可点开日志快捷面板定位配置项。

**文件格式（`cache/log/system_tts.log` 每行）：**

```
time | LEVEL | configId | roleName | message
```

- `time`：`yyyy-MM-dd HH:mm:ss.SSS`
- `LEVEL`：ERROR/WARN/INFO/SUCCESS/DEBUG/VERBOSE
- `configId`：配置项 ID（无关条目为空段 `  `），供快捷面板定位
- `roleName`：实时角色名（仅多角色对话有值，空则为空段）
- `message`：日志正文（可含 `|`，解析时需按固定 5 段切分）

解析规则在 `TtsLogViewModel.toLogEntry()`：
1. 先匹配 `time | LEVEL | <数字configId> | roleName | msg` —— 正常请求日志
2. 再匹配 `time | LEVEL |  |  | msg` —— 系统级日志无 MDC 产生空段
3. 最后兼容旧三段 `time | LEVEL | msg`（消息里可含 `|`，需全量重组）

---

## 四、过滤逻辑（TtsLogViewModel.filteredLogs）

四维度过滤：

1. **日志级别** `selectedLevels`（默认空=全部显示）
2. **搜索词** `searchQuery`（匹配 message 或 time）
3. **插件日志开关** `showPluginLogs`（默认隐藏）
4. **朗读规则日志开关** `showSpeechRuleLogs`（默认隐藏）

> 归入系统日志（`isPluginLog=false`、`isSpeechRuleLog=false`）的条目**默认显示**，不受任何开关控制。

**错误旁路（09-09）**：不勾选插件/规则开关时，对应缓冲的 **ERROR/WARN 级条目仍按时间戳混排进主时间流**——默认视图下插件/朗读规则出问题直接红黄字可见，不必开开关排查；勾选则该缓冲全量混排；都不勾且无旁路条目=纯主列表直通。

**连续同模式折叠（09-09）**：`routeEntry` 对插件/规则缓冲做折叠——消息数字归一化（`\d+`→`#`）后与缓冲末条相同则并入末条（`repeatCount+1`，`message`/`time` 取最新），不新增条目。渲染端在行尾追加「×N」。只作用于插件/规则缓冲，主列表不折叠。`LogEntry.repeatCount` 默认 1。

**正文着色（09-09，`LogScreen.kt` 来源优先）**：

| 条目 | 颜色（浅色主题 / 深色主题） |
|---|---|
| 插件 INFO | 灰青 `#4E6E68` / `#8FA9A3` |
| 插件 DEBUG/TRACE | 淡灰青 `#829895` / `#75908A` |
| 朗读规则 INFO | 灰紫 `#6E5E78` / `#A795B1` |
| 朗读规则 DEBUG/TRACE | 淡灰紫 `#9487A0` / `#90819F` |
| ERROR / WARN | 红 `#F44336` / 黄 `#FFC107`（级别语义，不动） |
| SUCCESS | 整行石板灰（同「获取成功」前缀） |
| 系统 INFO / DEBUG / TRACE | 绿 `#4CAF50` / 蓝 `#2196F3` / 灰 `#9E9E9E`（不变） |

> 设计原则：**色相=来源，亮暗红黄=级别**。功能日志（插件/规则）存在感介于主流程绿与石板灰之间，避开发音人棕褐 `#7D6B5D`。

## 五、持久化

- 只有 `SystemTtsService.logS()`（SUCCESS 级别）会写文件 `cache/log/system_tts.log`
- 插件日志、朗读规则日志只在内存中，不持久化
- `TtsLogViewModel.pull()` 启动时从文件读取最近 1500 行恢复
- `clear()` 清空内存列表并清空文件

## 六、关键文件速查

| 文件 | 作用 |
|------|------|
| `app/.../compose/systts/TtsLogScreen.kt` | 日志栏 UI（TopAppBar + 筛选 + 列表） |
| `app/.../compose/systts/TtsLogViewModel.kt` | 日志收集、过滤、持久化 |
| `app/.../compose/systts/LogScreen.kt` | LazyColumn 渲染日志条目 |
| `app/.../compose/systts/LogQuickPanel.kt` | 日志快捷面板（发音人调整/试听/音频参数） |
| `app/.../compose/systts/LogFilterDialog.kt` | 筛选对话框（级别 + 开关） |
| `app/.../SysttsLogger.kt` | 系统 TTS 日志单例 |
| `app/.../SysttsFilter.kt` | Logback 过滤器（logger 名白名单） |
| `lib-script/.../console/Console.kt` | 脚本日志（PLUGIN / SPEECH_RULE） |
| `lib-common/.../LogEntry.kt` | 日志数据模型 |
| `lib-common/.../LogLevel.kt` | 级别定义、颜色映射 |
| `app/.../compose/SegmentedTextToggle.kt` | 分段切换自制组件（宽度适配文字、无对勾） |
| `lib-tts/.../TaggedTtsPreviewPlayer.kt` | 统一试听链（状态机 IDLE/SYNTHESIZING/PLAYING） |

## 七、旁路日志组件（非日志栏主路径）

- `LoggerBottomSheet.kt`：代码编辑器底部日志面板，用实例级监听器（非全局），只显示单个 Console 实例的日志
- `LoggerFloatingScreen.kt` / `LoggerFloatingManager.kt`：悬浮窗日志，独立系统
