# mumu_tts_github 分支修改日志

**对比基准**：TTS-Server-v1.26.08220706.apk（tag `1.26.08220706`）
**当前版本**：`d45af73`（2026-08-30 晚）

> 本版在 0828 版（`6122f42`）基础上累计推送（`6122f42..26de2ab` 25 个提交，`26de2ab..d45af73` 再追加 16 个），标注【新增】的为本轮内容，【定版】为对旧描述的就地改写；被本轮推翻的旧描述已就地更新为最终态。

---

## 一、JRead 兼容

- **JRead 插件格式全兼容**：支持 JRead 插件双格式导入导出，内置运行时垫层，修复语速刻度与 http 超时参数（dfb6004）
- **JRead 声音引擎配置列表导入**：支持 `jread_voice_config_bundle` 格式（c3b5fd0）
- **JRead 通用标签自动映射**：旁白式/斜杠式/主角式标签自动映射为 mumu 标签，补零规范化（598ea6f、732ecd1）
- **JRead 分组名转换**：长名式/群杂式/斜杠式映射短名，其余原样保留；后续收敛为仅年龄段长名与特殊转短名（74b9b29、b2043be）
- **JRead 三级分组照搬**：一级组名作 mumu 分组（同名复用），二三级留在 categoryPath（c35a71d）
- **修复 V2 插件探测 ttsrv 报「JavaScript 值无效」**：引擎 getter 规范化 + 垫层 JS 门面遮蔽（81e2b05）
- **修复 Rhino defineProperty 类型不匹配**：defineGetter 改用 Supplier（b23a49f）
- **子分组头操作聚合整棵子树**：勾选/删除/导出/整理标签均覆盖下级层级（418be7b）
- **修复 JRead 一级组名分桶导入丢失**（f45e965）
- **导入修复**：`data` 支持对象写法；子分组斜杠分段＋去重；主角标签空格归一；性格/群杂标签原样保留（3c4fe43）
- **特殊标签定版**：分组名斜杠式整体映射（女/女童、男/特殊）；子分组内嵌斜杠先整体映射再拆段（f01d1b7）；最终按实际标签库收紧——性格/尾缀分组整条原样保留不再映射，特殊标签补零、仅男主不补零（57ac0b8）
- **多层子分组层级区分**：子分组头按层级缩进且背景递减，深层配置项同步缩进对齐（8de6b45）
- **tag/tagName 双轨写入【新增】**：tag=内部键、tagName=软件显示名（tag=narration 显示「旁白」）；导入时经 `tagNameFor()` 查规则 tags 表一并写入（9a5a9aa）
- **四括号显示名映射【新增】**：【】括号发音人→括号1、在线音效→括号2、「」括号发音人→括号3、『对话旁白』/『』括号发音人→括号4，双向（9a5a9aa）
- **映射边界补全【新增】**：长名特殊式「女性青年/特殊10→特殊女10」（剪映导出形态）；分组名「女特殊/男特殊→特殊女/特殊男」（7c210de）
- **导入自动绑定启用规则【新增】**：tagRuleId 为空会导致编辑页提示「未绑定朗读规则，无法切换标签」；导入时绑定当前启用规则（cafebac），旧数据在下一次导入触发的迁移中自动回填并重算显示名（48d6cec）
- **规则外标签显示名防误标「旁白」【新增】**：规则 JS `getTagName` 对表外标签（群杂/性格/remoteSound）兜底返回「旁白」，规则绑定回填后这些项的显示名被整体误标——现 app 侧对表外标签跳过 JS 重算、原样保留显示名（a139ef8）
- **升级后首启自动修复【新增】**：装新版后首次启动一次性强制重算全部分组显示名（未知标签回写原始 tag），存量误标项无需再导入即自动恢复（d45af73）
- **映射/分池/整理全流程总结文档**：`技术文档/jread映射与分池规则.md`（1de20c8）

## 二、合并分组（新功能）

一级分组菜单新增「**合并到其他分组**」：

- 源分组**全部配置项**迁入目标分组，迁完自动删除源分组
- 分类路径按**关键词归一化**归堆：纳米/女青年 → 剪映/女青年（支持男女童少青中老、男主女主、特殊男女共 14 个关键词）
- 目标没有对应分类的项保留原路径，作为新子分类，不丢项
- 「**插入到开头**」开关：想优先用新插件声音时勾选，默认追加末尾
- 迁移后仅对**受影响分类**重新编号，无关分类不动；编号规则与朗读规则一致（女青年01 补零、男主1 不补零）
- 子分组定义随合并同步迁移：改名/移动/转出候选并入 JSON 键一起搬走（f09ea6c）
- **菜单形态回调**：一级分组菜单恢复 DropdownMenu 弹窗，撤销 ModalBottomSheet 实验；功能更名「**合并同类配置项到其他分组**」，文案明确空分组自动删除（573f84d）
- **弹窗副标题迭代**：先分行逐条列清合并方式/配置项去向/原一级分组与子分组处理（c2481f6），再按标签类型表述两种去向（63e8f3a），最终精简为一句：同关键词归并、其余转新子分组（f8e2730）

## 三、分组界面优化

- **一级分组序号徽章**：名字前显示主题色小序号方块，按列表位置自动编号，拖拽排序后自动重编，替代手动 emoji 序号；后续降饱和改用 surfaceVariant，纯信息元素不再抢交互元素注意力（99b5a6c）
- **修改子分组前缀**：一级分组菜单新增，批量添加/替换/去除子分组名前缀（查找留空=添加，替换留空=去掉），实时预览，音频参数随键名同步迁移；后续支持自动识别公共前缀并简化为单输入框（90e8cde）
- **空子分组可见化**：仅存于参数 JSON 的空子分组也渲染组头，可勾选/改名/删除/移出；新建或转子分组后立即可见，空一级组转子分组有目标提示且转换后删原组（ce66789、f09ea6c、37b35f9、86b11d1、1e8de1a）
- **空一级分组修复**：放宽确认按钮并注册空子分组的音频参数字典键，修复无法新建子分组的问题（071b7ef）；菜单随之精简，隐藏针对配置项/子分组的无效操作项（bf0d379）
- **空子分组菜单精简**：空子分组三点菜单隐藏项相关操作（删除启用/未启用配置、移动启用配置、排序、导出），仅保留结构类操作；空子分组长按不再弹空菜单（722c536）
- **空子分组不显示修复**：子分组树缓存签名纳入 `subGroupAudioParamsJson`，修复新建空子分组时因签名不变导致树不重建、空子分组被隐藏（ee94cd1）
- **「编辑分组」更名「从其他分组移入配置」**：明确与长按移出的进出关系（66722a5、aa31dd0、1ec16a7）
- **一级分组菜单形态**：主菜单与长按扩展菜单曾改为 ModalBottomSheet 从底部弹出（073ad83、91cb1cb）；本轮撤销，恢复 DropdownMenu 弹窗（573f84d，见「二」）
- **标签快捷整理图标**：一级分组组名后新增快捷图标，点击直接整理全部子分组标签（58a26d8）；子分组名后同样新增，一键整理本子分组标签（016d8fa）；两者均带非空门控，分组无配置项时不显示，且与整理标签菜单项统一门控（0de2b65、2338c8d）
- **整理入口门控**：系统 TTS 列表三点菜单的「整理全部子分组标签」仅在至少一个子分组名含关键词时显示（c359c98），与标签快捷图标同批加入（见上一条）
- **标签重排跳过优化**：一键分配标签时已正确的项直接跳过，1600 人大分组全排好时 0 次 JS 计算、0 次写库（含后续编译修复 c1bea68）
- **真实计数提示**：重排完成提示实际修改数，全部已排好时提示「无需整理」，不再虚报总数
- **快捷整理图标优化**：移至行尾操作区并升级为标准 IconButton，增大尺寸与触控区（12edfb6）
- **快捷整理二次确认**：快捷整理按钮加二次确认弹窗，防止误触批量重编号标签（a4aa934）；确认弹窗提示词精简（6122f42）
- **简化多选删除流程**：去掉重复选分组弹窗和二次确认，改为一步确认即删除——选中分组 → 点删除 → 弹「确定删除 N 个分组？」→ 确认（5d07093）
- **快捷整理图标定版为琥珀金圆底按钮【新增】**：38dp 主题金圆底 + 21dp 魔棒图标（OrganizeWandColor #B8862B 专属色，固定不随主题），取代此前的裸图标样式（43fdd64、12edfb6）；大分组行与子分组行统一（e83e5e9）
- **行内魔棒快捷键全部移除【定版】**：上述行内魔棒（曾改 36dp 触控 + 20dp 琥珀金无底 IconButton，d1b1e4a）最终整体删除——单击即整理无确认，易误触批量改标签；一级分组行魔棒连同其二次确认弹窗一并移除（ab0f30c、d9b14fb）。**整理入口只保留在菜单**：子分组菜单「一键整理标签」、一级分组菜单「整理全部子分组标签」
- **组行树图标恢复原位置形状【定版】**：含子分组的一级分组，树图标曾随名称左移为 15dp 弱化小图标（b963380），最终恢复行右侧原位、18dp 主题色（398cc24）
- **心声芯片防重置【新增】**：编辑页点亮「心声(内心独白)」芯片不再被标签下拉强制重置（保留标签追加进候选，也可从下拉选回）；点亮时显示机制说明文案（5287012）
- **三态勾选框半选单击直达取消【新增】**：部分选中分组单击直接整组取消，不再经过「先全选」中间态——旧中间态会触发跨分组标签去重，把其他分组同标签已启用项顶掉且不恢复（b971814）
- **大分组展开轻量化【新增】**：展开状态从分组表 isExpanded 列改为 DataStore 轻量集合，切换零写库——修复大分组（千项级）展开/折叠明显卡顿（写库触发 Room 全量重发 → 分池判定 + 全部分组树重建）；旧展开状态启动时自动迁移（5fca19c）
- **批量删除失效配置后自动清空分组【新增】**：删完失效项后同一事务内遍历分组，变空的分组自动删除（默认分组豁免）；平时手动清空的空分组照常保留（ab0f30c）
- **默认分组按需显示【新增】**：默认分组（id=1，代码兜底用）仅在有配置项时显示，空着不再在列表里出现；分组记录仍保留在库中兜底（250abdd）
- **列表首屏提速【新增】**：顺序整理（updateAllOrder）挪出列表加载链路不再阻塞首查；进程内缓存上次列表，同进程再进页秒显；首查完成前显示加载中而非误导性的「暂无分组」（4ae44cc）
- **关键词整理排除「女性少年/男性少年」【新增】**：这两个长名分组名含连续「少年」会误触发少年整理，已明确排除，仅「少年」名可触发（d9b14fb）
- **清理已废弃朗读规则 JS【新增】**：移除 2.85 双版本与旧 2.87 三个过时文件（181f348）

## 四、列表分池

- **通用池 / 高级池页签**：系统 TTS 列表按嵌套层级分池，多级嵌套分组自动归入高级池，两池搜索与多选状态相互独立（56004e0）
- **高级池为空时隐藏页签**，界面与普通列表完全一致（b0aab7b）
- **分池判据定版为按朗读标签【定版】**：不再看分组名/categoryPath/参数字典键——组内全部配置项的标签在朗读规则 tags 表内、可被 jread 标签转换覆盖、或为空白 → 通用池；性格词、群杂式、remoteSound 等规则外标签 → 高级池（4213bd3 修复解构顺序，7c210de 定版；取代 0f72ead 的路径判据）
- **补零规则定版【新增】**：仅 1–9 补成 01–09，10 及以上（含三位数 100/517）原样；男主始终不补零（7c210de）
- **旁白分组一键整理【新增】**：旁白子分组可整理，不带序号统一 narration/旁白，跳过已正确项（d599f00）
- **一键整理只产出合法标签【新增→已回退】**：前缀须落在绑定规则 tags 表内的校验（2d95486）实测误伤后整体回退（ab0f30c），整理不再做规则表校验
- **旁白整理防误转【定版】**：旁白整理只统一旁白族标签（旁白X/narration/空白）的项，人物标签（如 jread 的 女青年01）即使出现在旁白命名的分组里也不改——修复一键整理把人物标签拍平成「旁白」的问题（ab0f30c）

## 五、编辑页

- **按语言自动分组**：批量保存声音时可开启，未手动分配分类的声音以当前语言显示名作分类路径；开关仅在插件含多种语言时显示（dc1e347、bd736ff）。**该开关现已移除**（f0186b3）：中途曾放宽为单语言插件也显示（60401cb），最终由插件页「按分类入库·全部分类」取代，分类名直接映射标准名
- **标签选择改两层弹窗【新增】**：点击标签字段弹出「分类 → 序号」两层选择器（与列表页标签切换同一交互，可复用 TagPickerDialog 构件）；打开前自动标签扩容；心声芯片点亮时字段显示「心声(内心独白)」（b79015e、3c96be3）
- **标签区重叠修复【新增】**：「心声(内心独白)」芯片此前压在规则脚本选择器上——根因是 AnimatedVisibility 内容为堆叠布局（非竖排），芯片/选择器/说明文字须包 Column；另规则脚本选中值恒定单行，超长规则名不再撑爆字段（5e9cd0e、07c1164）

## 六、导入体验

- **统一四类导入成功文案**：修复跨类型导入只显示类型名的问题（37ea66a）
- **跳过项按原因分类计数**：缺少对应插件 / URL 直连型分开统计（16af9f4）
- **导入提示语序定版【新增】**：改为先分组后条数「已导入 x 组替换规则 x 条」「已导入 x 组配置列表 y 项」（ce5ea5f），再加逗号调整为书面语序「已导入 x 组…，共 y 条/项」（da1fe04）

## 七、插件管理

- **删除插件双按钮弹窗**：显示插件被引用数，可「仅删插件」或「连配置全部删除」（54fe6e9）
- **切换插件联动**：编辑区清残留状态、displayName 跟随、自动选语言拉取声音（5e10ddb）
- **失效配置按来源删除**：失效配置详情每个来源加删除按钮，批量清理该来源失效项（b965c4f）
- **失效配置详情行布局优化**：切换插件与删除按钮移至第二行右对齐，第一行完整显示分组名；操作改轻量文本样式，修复此前分组名被挤成省略号的问题（30213cf、aff91c1）
- **按分类入库【本轮重构】**：仍由插件管理页 ⋮ 菜单进入，Rhino 单线程遍历插件 getLocales→getVoices 按原始分类批量导入新配置条目（默认停用），样本率经合成实测回填（bb7dab0、51c7303、8af8c8c、1650d23）。本轮演进为最终态：**自动按插件名建分组**（不占用已有分组）、显示**分类勾选列表和映射预览**（11f841f）；标准人群分类映射归并入对应子分组并打标签，性格类等无映射的分类原样入库不打标签（cb06ce9）。期间曾短暂去掉映射直接使用插件自身分类名入库（de53cf5），随后已被上述方案取代
- **更名「按插件音色分类入库」+ 对话框重排【新增】**：功能名精确化；插件名挪出标题作次级信息行（两行省略）——超长插件名不再把大字标题撑出五六行；全选行与分类行共用同一行构件，勾选框绝对同列；映射预览同行右对齐（70c1e69、0f85490）

## 八、设置页

- **全局搜索框**：设置页顶部输入即过滤，覆盖全部设置项（7f6ee90）
- **搜索框定版嵌入顶栏标题右侧【定版】**：曾移入顶栏搜索模式（2851d31）、曾改回常驻顶部搜索框（4213bd3），最终定版为「设置」标题右侧紧凑搜索框——无描边、全圆角、surfaceContainer 底（与底部导航栏同色）、占位「搜索设置项」、右侧留边不贴屏幕；输入即过滤全部设置项（4a30c96、26de2ab）
- **搜索框换日志页同款 InputField【定版】**：自绘胶囊曾出现占位文字纵向裁字（OutlinedTextField 内部最小 56dp 被压 48dp，f8bc65a 先改自绘修复）与文字垂直偏上（顶栏标题槽 ambient 是 titleLarge 行高 28sp，只改字号继承大行高，a98baea）；最终整体换用日志页「搜索日志」同款 M3 `SearchBarDefaults.InputField`——自带垂直居中、全圆角胶囊、surfaceContainer 底、✕ 居右清除，并加放大镜 leadingIcon，长度仍占标题右侧剩余宽度（0de2883、429af27）

## 九、界面样式

- 发音人颜色调整为棕褐 #7D6B5D（深色 #A08B7A）（e2db455）
- 日志栏「获取成功」前缀加粗，与「大小·耗时」视觉统一（c556a6f、a4d2abd）

## 十、心声 AI（标签识别与密钥）

- **心声标签正则分类器**：TextProcessor 集成正则归类旁白/心声标签（d6ee8c4、b5a0ff3）
- **心声 AI 兜底判定**：OpenAI 兼容端点判定 + LRU 缓存 + 熔断保护；设置页新增 AI 配置区；编辑页心声快捷 tag（0f3c8c2）
- **AI 配置一键导入密钥**：对话框支持从角色管理密钥文件直接导入（3ef7320）
- **密钥源唯一化为 miyue.txt**：多角色朗读格式，对齐 2.87 parseSingleGroup 解析语义；最终态删除 DB/key_list.json 旁路。中间历经多次调整：直读 key_list.json→降级兜底→按「当前密钥」名精确匹配→最终统一单源（a41d9da、df907a2、18d349a、6404cf9、b1840ef）
- **判定文案精简**：设置页心声 AI 判定文案先做整体简化（edebbce），再进一步删冗余状态行、清理无用 import（8b7a2ef）

## 十一、心声混响（新音效）

- **ReverbAudioProcessor 音效链**：播放主链与 stackCore 均接线，透传配置项自身参数（2d356aa、87a0ed2、96f0547、1094cf2）
- **编辑页混响开关**：audioParams_reverbEnabled 字段，Room 手动迁移 v31→v33 稳定落地（4266d6f、ff98898、96f0547、1094cf2）

## 十二、打包脚本

- release.yml 支持 `1.26.*` 格式 tag 推送自动打正式包，与手动触发共存（bdd046b）
- **修复 release 包混元太极闪退**：添加 Ktor 和 deepseekproxy 混淆 keep 规则，捕获 Throwable 兜底 NoClassDefFoundError（657491f）

## 十三、混元太极（新增功能）

- **崩溃捕获黑匣子**：全局未捕获异常处理器把堆栈写入 `crash_last.txt`，下次进混元太极页自动弹窗展示完整堆栈，支持一键复制；看门狗主动重启同样留痕，与真实崩溃区分（df984af）
- **后台拉起闪退加固**：App 进程被系统在后台拉起时（如 TTS 引擎绑定），自动恢复混元太极前台服务在 Android 12+ 上会因后台禁启前台服务抛 `ForegroundServiceStartNotAllowedException` 导致闪退，现已捕获降级为日志记录（092d217）
- **插件引擎 ttsrv 常量冲突修复**：`defineGetter` 去掉 `PERMANENT` 标志，避免 Rhino 将 `ttsrv` 视为 `const` 导致 `initScript` 重声明冲突（ad742bd）

---

*共 154 个提交（基准版本起累计；`6122f42..26de2ab` 25 个 + `26de2ab..d45af73` 追加 16 个，含 2 笔 jread 显示名护栏与首启修复）*
