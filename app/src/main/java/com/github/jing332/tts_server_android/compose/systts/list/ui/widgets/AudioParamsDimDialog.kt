package com.github.jing332.tts_server_android.compose.systts.list.ui.widgets

/**
 * 本文件原声明 AudioParamsDimDialog（编辑页单维音频参数弹窗：只有该维三层滑杆 + 重置/应用 + ▶试听）。
 *
 * 已于 2026-09-10 晚撤销（用户定稿）：编辑页三键直出改为与卡片⋮入口**共用同一个** `AudioParamsDialog`
 * ——顶部同样有 发音人 + ▶试听 + 终值行，主体是维度软槽 + 该维三层滑杆 + 重置/应用，
 * 点哪个键就以哪个维度打开（`initialDim`）。三键行本体已迁到 `AudioParamsDimChipsRow.kt`。
 *
 * 本文件保留为空占位（无任何声明），待后续清理时可整体删除。
 * ⚠️ 注意：本机 WorkBuddy 环境里 `git rm` 会触发工作区整树批量删除（索引无损、`git checkout -- .` 可恢复），
 * 删除文件请改用别的通道（用户 IDE / GitHub API），勿在本环境用 `git rm`。
 */
